## 2026-07-17 工作记录

### 项目重启：现状盘点 + 三角色诊断
- 停滞于 2026-07-08，工作树遗留两条未提交线：Scholar OS UI 改版、双机同步方案 v0.2。
- 数据快照：works 150（parse 全部 succeeded）；元数据 approved 119（31 篇缺口 + 11 pending）；分类 approved 119；intake 12 pending（6 approved 未 promote）；主题 2 个 seedling；analysis_runs 仅 5 条 pilot；_inbox 躺 1 篇 PDF。
- 核心诊断：入口侧设施 95 分、出口侧（引用导出/综述矩阵/分析）几乎未启用；"流程没打通"= 各页面间无接力引导，非缺功能。需求不确定的根因是系统未进入真实使用阶段。

### 用户三项决策
1. UI 改版：直接完成并提交 ✅（commit 7689813）
2. 双机同步：冻结，git+网盘兜底 ✅（规划书头部已标注，FUTURE_WORK_PLAN 已记录）
3. 下一阶段方向：驾驶舱（Dashboard 待办面板+流程接力）→ 出口侧（引用导出/综述矩阵），入口侧新功能暂停

### 清场完成（commit 7689813 / 25b097f / 6fddff5）
- Scholar OS 视觉改版提交（AppIcon + AppLayout 重写 + Dashboard/Works/StageCard/App/router/index.html）
- 修复 PipelineView ambLevel 阈值 bug（0.7/0.4 → 50/20，badge 全"高"问题）
- TECHNICAL_OVERVIEW 版本标注 V1.1→V1.3 + 滞后提示；删除杂散文件 nul
- 回归：pytest 533 passed / healthcheck 全空 / check_docs PASS / npm build 通过

### 待办（下一步）
- 用户实看 UI 改版效果（npm run dev），确认品牌文案（Scholar OS / GPT-5.6 协作设计）是否保留
- 第 1 步驾驶舱：Dashboard 今日待办深化 + 审核页"下一步"接力引导 + 清积压（31 篇补审、12 候选、inbox 1 篇）
- 第 2 步出口侧：BibTeX/RIS 引用导出 + 综述矩阵导出（FUTURE_WORK_PLAN #10）

### 文档一致性审计 + 修复（同日，commit 634c0bd/c728900/1d2670c）
- 双路只读审计（顶层权威文档 + 手册/专题文档），漂移集中三类：TECHNICAL_OVERVIEW 滞后、HANDOVER 两条校准旧账（UX-001/QA-004）、Scholar OS 改版带来的术语层漂移。
- 已修：TECHNICAL_OVERVIEW 全面刷新（11 路由组/15 页面/补 28 端点/Ollama 标弃用）；HANDOVER 页面全景重画；/dashboard→/；USER_ISSUES UX-001 移入已解决+补阈值修复验收；risk_domain 补 reward_hacking；scripts/README 补登脚本（migrate_* 用户决策保留）。
- 已归档：两份 superpowers review → _archive/superpowers/reviews/；ambiguity issue → _archive/issues/；删除 docs/plans、docs/reviews、docs/uperpowers 空目录（check_docs warning 归零）。
- 代码修复：router.js 14 条路由 meta.group 对齐新导航（顶栏 eyebrow 曾显示旧分组）。
- PROJECT_INDEX 本地标注"已取代"（该目录被 gitignore，不入库）；update_index.sh 实际存在于根目录但 2026-06-07 后未运行。
- 回归：pytest 533 passed / healthcheck 全空 / check_docs PASS 无 warning / npm build 通过。
- 已知残留：TECHNICAL_OVERVIEW 正文仍有 Ollama 历史角色描述（属准确历史表述，有意保留）；router meta.title 与导航标签措辞略异（"流程管理"vs"处理流程"，未对齐，低优先）。

### 品牌与模型贡献署名（同日，用户测试后决策）
- 用户实看界面满意：Scholar OS 品牌**保留**；删除所有 "GPT‑5.6 designed / 协作设计 GPT‑5.6" 署名（AppLayout brand-copy、model-card、Dashboard hero-kicker）。
- 侧边栏左下角新增"模型贡献"一行 + title 悬浮详情：早期设计 MiMo-2.5 Pro；质量控制 GPT-5.5 · Claude Sonnet 4.6；界面设计含 GPT-5.6；抽取主力 MiMo-2.5 Pro；当前主力 Kimi 3（GPT-5.6 辅助审核）。
- 用户远期意向（勿忘）：后续可能扩充设计 agent，用于检索与文献综述，借助已有 agentharness 和 skills——属后期规划调研，未排期。
- 状态：改动未提交，等用户确认后入库。

### 架构/流程图 three.js 重绘（同日，commit 见 git log）
- 用户确认品牌改动（已提交 style commit 2ab9515）。
- 新建 `docs/architecture/scholar-os-architecture.html`：three.js r150 交互式 3D 架构图（vendored 到 `docs/architecture/vendor/three.min.js`，无 npm 依赖，file:// 双击可开）。
  - 架构总览：6 分区（入口/闸门/入库/解析/AI抽取/审核）18 节点 + 治理旁路 + SQLite/文件区底座，节点呼吸发光、边粒子流动、点击节点看职责。
  - 流程 A（本地入库 10 步）/ 流程 B（发现检索 13 步）动画导览：压暗非路径节点，packet 沿路径飞行，底部解说条可暂停/步进；支持 `?mode=A|B` 深链。
  - 坑：jsdelivr 下载 three.min.js 曾截断（211KB/613KB）导致 THREE undefined，重下后恢复；无头截图验证用 Chrome `--enable-unsafe-swiftshader --use-angle=swiftshader`。
- 原 8 张顶层 SVG（系统总图+7 流程图）git mv 至 `docs/_archive/architecture/diagrams-2026-06/`（未删除）；子项目 5 张 SVG 保留。
- system-architecture.md / business-flows.md / architecture README 引用已同步；check_docs PASS。
- 维护约定更新：系统级图以交互页为准，改架构事实需同步页面 NODES/EDGES/PATHS 数据。

### 交互页实现状态核查（同日，commit 413c1a6）
- 18 节点逐一对照代码：17 完整实现；分析出口部分实现（digest 试点有，引用导出/综述矩阵无）。
- 新增 status 视觉编码：partial=琥珀虚线环+角标，planned=灰虚线（预留）。
- **字段级重抽确认已实现**（用户以为还在规划）：api/routes/metadata.py rerun-preview/apply/prompt + MetadataReview 字段行按钮 + tests/test_metadata_rerun_api.py。

### 当前主线三阶段确立（同日深夜，用户决策）
- 整体路线图落位 `FUTURE_WORK_PLAN.md` 新增「当前主线」一节（不新开长期文档，遵循既有规划层级治理）。
- 阶段一 出口侧最小闭环（BibTeX/RIS 引用导出 + 综述矩阵，#10 提前，数据源仅 approved 119 篇）→ 阶段二 驾驶舱与流程接力（#6 + 清积压）→ 阶段三 反馈环月报脚本（A.6 机制化，复用 analysis_runs）。
- 入口侧新功能冻结（#1/QA-004/UX-005 等），双机同步维持冻结；每阶段开工前新建 docs/superpowers/plans/ 实施方案。
- 全部工作已 push 到 origin/master。
