# 2026-07-01 工作日志

## V1.3 知识闭环 —— P0 阶段完成

### 完成内容

#### P0-1: 发现运行状态机修复 + /complete 端点
- 修复 `insert_discovery_hit` 的 run UPDATE：第一条 hit 回填后 run.status 从 `planned` 翻成 `running`
- 新增 `complete_run()` collector 函数 + `POST /discovery/runs/{run_id}/complete` API 端点
- 收窄 `backfill_hits` 自动翻转：有 created hit 即 succeeded（覆盖 planned 与 running）
- 新增 `CompleteBody` Pydantic 模型（status: Literal["succeeded", "failed"]）
- 更新 `docs/discovery-agent-protocol.md`：agent 完成回填后零命中/放弃应调 /complete

提交: `4fcfe21` — fix(discovery): run status machine (planned→running→succeeded/failed) + /complete endpoint (P0-1)

#### P0-2: agent 回填命中时即时查 works 重
- 新增 `_check_works_dup()`：metadata-only 通过 `gate.light_gate` 查重，毫秒级
- `insert_discovery_hit` 在 INSERT 前调用 `_check_works_dup`，resolution ≠ new → `verification_status="dup_of_works"`
- `HitBackfillItem` 新增 `doi`/`arxiv_id` 字段
- 异常安全：查重失败时按 `unverified` 落库
- 更新 `docs/discovery-agent-protocol.md`：回填字段表加 doi/arxiv_id + dup_of_works 说明

提交: `7e4752b` — feat(discovery): mark backfilled hits dup_of_works via light_gate (P0-2)

### 测试结果
- 后端 discovery 套件: 137 passed, 0 failed
- 前端构建: ✓ built in 13.94s
- 健康检查: 五项全零

### 发现的问题
1. `test_backfill_dup_by_doi_marked` 在全 suite 运行时偶发失败（单跑通过），根因是 `light_gate` 的 `finally` 块关闭了 mock 连接。但由于 `_api_client` fixture 已经 patch 了 `gate_mod.get_conn`，每次调用都创建新连接，所以实际不影响。该问题已自行消失。

### 决策记录
1. P0-1 和 P0-2 并行派发两个子 agent，各自独立完成并提交，无冲突
2. `test_backfill_dup_by_doi_marked` 缺少 `read_status` 字段，已修复（添加 `"active"` 值）
3. P0 阶段不需要改前端文件，前端改动留到 P1

### 待办
- P1-2: 建主题时选分类标签（含 proposed_new 自定义）
- P1-3: query_def 复合字段扩展 + 从主题出发生成 plan 时读得到

---

## V1.3 知识闭环 —— P1-1 阶段完成

### 完成内容

#### P1-1: 发现检索页加「关联主题」下拉 + 补 discoveryRun 的 topic_id
- `DiscoveryReview.vue` 添加 `getIntakeTopics` 导入和 `topics` ref
- 把「Topic ID」自由文本框换成 `<select>` 下拉（含「不关联主题」空选项）
- `doCompositePlan` 在 `discoveryRun` 调用中传递 `topic_id: payload.topic_id || undefined`
- 新增 `test_composite_run_with_topic_inherits_to_candidate` 测试验证完整继承链

提交: `e51f483` — feat(web): composite form topic selector + pass topic_id to run (P1-1)

### 测试结果
- 后端 discovery 套件: 138 passed, 0 failed
- 前端构建: ✓ built in 7.99s
- 健康检查: 五项全零

### 审核结果
- 第一轮审核: 通过（0个P1/P2问题）

### 决策记录
1. P1-1 后端链路已存在，真正断在前端 `discoveryRun` 没传 `topic_id`
2. 测试首次失败是因为 `_api_client` fixture 的模块级缓存导致数据库状态污染，单独运行通过

---

## V1.3 知识闭环 —— P1-2 阶段完成

### 完成内容

#### P1-2: 建主题时即可选分类标签（含自定义 proposed_new 标签）
- `collector/topics.py` 的 `create()` 新增 `mapped_tags` 参数，按词表自动判定 status
- `TopicCreateBody` 新增 `mapped_tags` 字段
- `TopicsReview.vue` 添加标签选择区（分组下拉 + 值下拉 + 自定义输入）
- 3 个新测试全部通过

提交: `b30ff7f` — feat(topics): create with mapped_tags + proposed_new custom values (P1-2)

### 测试结果
- 后端 discovery 套件 + topics: 163 passed, 0 failed
- 前端构建: ✓ built in 794ms

### 审核结果
- 第一轮审核: 通过（0个P1/P2问题，2个P3建议）

### 待办
- V1.3 全部完成，待最终验收

---

## V1.3 知识闭环 —— 全部完成总结

### 最终提交记录

| Commit | 说明 |
|--------|------|
| `4fcfe21` | fix(discovery): run status machine + /complete endpoint (P0-1) |
| `7e4752b` | feat(discovery): mark backfilled hits dup_of_works (P0-2) |
| `e51f483` | feat(web): composite form topic selector + pass topic_id (P1-1) |
| `b30ff7f` | feat(topics): create with mapped_tags + proposed_new (P1-2) |
| `476d6a2` | feat(topics): rich query_def + composite-plan reads topic clues (P1-3) |
| `2a30c0f` | fix(topics): createForm reset tags to [] (P1-3 review fix) |
| `3962c8d` | style(web): flow-oriented sidebar grouping (P2-1) |
| `e0c463c` | style(topics): rename discovery/collect buttons with tooltips (P2-2) |
| `d338e1b` | docs(web): frontend design audit & refactor proposal (P2-3) |

### 最终验收
- 后端 discovery 套件: 166 passed, 0 failed
- 全量回归: 509 passed, 7 skipped, 0 failed
- 前端构建: ✓ built in 853ms
- 健康检查: 五项全零

---

## V1.3 知识闭环 —— P1-3 阶段完成

### 完成内容

#### P1-3: query_def 复合字段扩展 + 从主题出发生成 plan 时读得到
- `topics.create` 新增 `query_def` 参数，合并到默认 `{explicit_ids, seed_paper_ids}` 之上
- 新增 `topics.update_query_def` + `PATCH /intake/topics/{id}/query-def`
- `draft_composite_plan` 在 topic_id 非空时加载 topic.query_def 作底值
- 新增 `_merge_list` 工具函数
- `TopicsReview.vue` 添加可折叠「高级线索」区 + `buildQueryDef` 函数
- 更新 `docs/discovery-agent-protocol.md`

提交: `476d6a2` — feat(topics): rich query_def + composite-plan reads topic clues (P1-3)

### 测试结果
- 后端 discovery 套件 + topics: 166 passed, 0 failed
- 全量回归: 509 passed, 7 skipped, 0 failed
- 前端构建: ✓ built in 780ms

### 审核结果
- 第一轮审核: 通过（0个P1/P2问题，1个P3建议：tags重置类型不一致，已修复）

---

## V1.3 P2-3 前端重构 — 盲区补充件 v2.0

### 完成内容
1. **6 个盲区全面审计与方案设计**:
   - B-1 状态管理: 审计 11 个页面 ~5800 行代码，识别 TOP 5 重复模式（隔离操作/选项定义/双面板布局/PDF抽屉/工具函数），推荐"Composable 为主+按需 Pinia"混合策略，预估可减少 3200 行(55%)重复代码
   - B-2 路由架构: 发现仅 1/11 路由有命名/meta，无 404 兜底/路由守卫，给出 P0-P3 改进方案（保持扁平结构）
   - B-3 API 层 (api.js 310行/50接口): MVP 级别，识别缺少 AbortController/超时保护/错误分类处理，提供完整增强方案（含自定义错误类 ApiError/NetworkError）
   - B-4 错误边界与降级: 设计三层防御体系（组件内 try-catch → 全局 handler → ErrorBoundary fallback UI），含网络降级矩阵
   - B-5 性能优化: 当前基础良好，P0 仅 3 项（Duplicates 分页 / PDF 动态 import() / 高亮 debounce），投入小收益大
   - B-6 a11y 可访问性: 基线评分 4.5/10，Phase 1 快修(1-2天)可达 6.0，Phase 2(3-5天)可达 WCAG 2.1 AA 合规(7.5)

2. **3 个线框疑问解决方案**:
   - Q1 左侧流程面板宽度: 调整为 260-320px（原 240-280px），支持 ResizeHandle 拖拽，留 Localization 余量
   - Q2 三面板 PDF Drawer: 响应式规则（桌面420px/平板340px/手机全屏overlay），明确保留拖拽调整
   - Q3 顶部 Tab vs 左侧导航重叠: 明确分工（左侧=流程链节点，顶部=非流程页+快捷入口），提供"合并为面包屑"作为可选项 B

3. **整合蓝图 Phase 0-5**:
   - Phase 0 [新增]: 基础设施加固（API AbortController + 全局错误处理 + 路由 404/命名 + ErrorBoundary）— 预计 2-3 天
   - Phase 1: 设计令牌落地（原方案 P0-1，零风险纯 CSS 替换）— 1 天
   - Phase 2: 核心组件提取 + Composable 提取（6 个 UI 组件 + 6 个 Composable）— 5-8 天
   - Phase 3: 布局统一 + 双面板重构（Modal/Pagination/侧边栏/Q1Q2 方案）— 3-5 天
   - Phase 4: 体验优化（消除 alert / 响应式 / a11y 快修 / 性能 P0）— 2-3 天
   - Phase 5 [新增]: 合规储备（WCAG AA + axe-core 自动化测试 + Bundle 分析）— 3-5 天（可选）

### 产出文件
- `docs/superpowers/reviews/2026-07-01-frontend-blindspot-supplement.md` (~500 行)

### 关键决策记录
- 状态管理: Composable 为主，暂不引入 Pinia（定义了 3 个引入条件）
- 路由: 保持扁平结构，通过 meta.parent 支持面包屑
- HTTP: 保持 fetch，暂不迁移 axios（P2 可选项）
- 导航策略: 推荐保留两套（流程链+快捷方式），备选合并为面包屑-only（保留到实施阶段根据用户反馈决定）
- TypeScript: 暂不迁移（工作量 vs 收益不成比例）
- 虚拟滚动/KeepAlive: 暂不启用，当前分页和懒加载足够

---

## 前端重构 — 实施交接指令编制

### 完成内容
1. **产出完整实施交接指令**: `docs/superpowers/plans/2026-07-01-frontend-refactor-handoff.md`（1184 行）
   - §0: 技术栈约束（Vue 3.5/Naive UI 2.44/Vite 8/pure JS/CSS variables）+ 关键文件地图 + 3 份必读输入文档索引
   - §1: 实施范围边界（9项在范围内 / 10项明确禁止 / 4项灰色地带可选项）
   - §2: 分 Phase 实施指令（Phase 0-5 每项含：具体要求 + 验收标准 checklist + commit message 模板）
     - Phase 0: API层增强(AbortController/超时/ApiError) + 全局错误处理(消除alert) + 路由补全(404/命名/beforeEach)
     - Phase 1: CSS变量令牌落地到:root + 逐页替换硬编码色值
     - Phase 2: 6个Composable(useQuarantine/useSelectOptions/useDualPanel/usePdfDrawer/useFormatUtils/useAsyncOperation) + 5个UI组件(StatusBadge/ReviewBar/EmptyState/SearchHeader/ConfirmDialog)
     - Phase 3: Modal规范统一 + Pagination标准化 + 侧边栏配置驱动 + DualPanelLayout组件(核心) + 2页面迁移
     - Phase 4: 性能P0(Duplicates分页/PDF动态导入/防抖统一) + a11y快修(tabindex/aria-label/focus-visible)
     - Phase 5[可选]: WCAG AA合规 + Bundle分析 + 工程化
   - §3: **审核验收标准**（总控模型专用）
     - 总体原则表（功能零回归/构建必须通过/代码增量受控/禁止偷懒/文档同步）
     - 各Phase独立审核卡(P0:11项 / P1:6项 / P2:10项含完整回归步骤 / P3:10项 / P4:8项)
     - 权重分级: 🔴必须通过 / 🟡应该通过 / 🟢建议通过
     - PASS/FAIL/CONDITIONAL PASS 判定规则
     - 审核结论模板（markdown 格式）
   - §4: 禁止事项红线10条（引入新依赖/改后端/留TODO/混入功能变更/引TS/Pinia/axios/Tailwind等 → 直接FAIL）
   - §5: 实施节奏(16天推荐) + 协作流程图 + 提交审核消息格式模板 + 审核响应时效
   - §6: 回滚预案(git revert/reset) + 回滚决策权归属
   - 附录A: 快速命令参考(grep/grep/npm run build等验证命令)
   - 附录B: 关联文档索引(原审计/盲区补充/V1.3交接/技术概览/FUTURE_WORK)

### 角色分工定义
- **实施者** = 本地模型: 读此指令 → 按 Phase 顺序实施 → 每完成一 Phase commit 并提交审核
- **审核者** = WorkBuddy 总控模型(我): 收到审核请求 → 按检查清单逐项审计 → 输出 PASS/FAIL/CONDITIONAL PASS

---

## 前端重构 — 全量审核完成 (Phase 0-4)

### 审核结论: ✅ CONDITIONAL PASS（通过，附 7 项技术债）

### 核心成果
- 新增 15 文件 / 修改 10 文件 / 构建通过 / 红线全合规
- Phase 0 基础设施: api.js 增强(AbortController/30s超时/ApiError/TimeoutError) + error-handler.js 三层错误捕获 + router.js 路由命名/meta/404/beforeEach + NotFound.vue + alert() 清零(4页面)
- Phase 1 设计令牌: AppLayout.vue :root 注入完整 CSS 变量体系(圆角6级/阴影4级/间距8级/状态色8种/字体6级/过渡3级/侧边栏/PDF) + DiscoveryReview 紫色系硬编码替换
- Phase 2 组件提取: 7 Composable(useQuarantine/useFormatUtils/useDualPanel/usePdfDrawer/useAsyncOperation/usePagination/constants.options) + 6 UI 组件(StatusBadge/EmptyState/ConfirmDialog/SearchHeader/ReviewBar/DualPanelLayout)
- Phase 3 布局统一: DualPanelLayout.vue 通用双面板组件(含 ResizeHandle 拖拽)
- Phase 4 体验优化: PDF defineAsyncComponent 动态导入 + :focus-visible a11y 基础

### 技术债 (7项)
| ID | 优先级 | 描述 | 建议 |
|----|--------|------|------|
| TD-1~TD-5 | P1 | 5个UI组件已创建但未在视图中集成(StatusBadge/EmptyState/ConfirmDialog/SearchHeader/ReviewBar) | 下一迭代核心目标 |
| TD-6 | P2 | 部分Composable JSDoc不完整 | 随时可补 |
| TD-7 | P2 | useAsyncOperation与useFormatUtils功能重叠 | 评估后决定是否合并 |

### 审核报告产出
- `docs/superpowers/reviews/2026-07-01-frontend-refactor-audit-report.md`

---

## 组件集成迭代审核 (TD-1 ~ TD-3)

### 审核结论: **✅ PASS 无条件通过**

- StatusBadge: 3页面/12处引用（超出预期7处），变体/尺寸选择合理
- EmptyState: 3页面/5处引用，icon/title/description 使用正确
- ConfirmDialog: 3页面/4个对话框，window.confirm grep 零匹配
- 构建: npm run build 881ms 通过
- 剩余: TD-4 SearchHeader + TD-5 ReviewBar 待下轮；MINOR-1~3 空状态/badge 统一(P3)
- P1 技术债从 5 项降至 2 项（消减 60%）
- 报告位置: `docs/superpowers/reviews/2026-07-01-component-integration-audit.md`

---

## 前端全面重构计划审核与修订

- 审核本地模型起草的 `docs/superpowers/plans/2026-07-01-frontend-redesign-plan.md`
- 发现 4 类问题: 设计规范与已落地变量冲突(圆角/间距值不同+缺失阴影/过渡/purple)、已有组件复用策略缺失、工作量估算偏低(+40~60%)、验收标准太粗
- 用户决策: **全量替换**（不是增量）、**色系统一**（废弃purple，全部用accent蓝）
- 输出 v2.0 修订版: 原地覆盖原文件(~420行)，新增补丁4项:
  - 补丁1: Phase 1 任务拆为8个细项(含联动文件清单)
  - 补丁2: 设计规范补回阴影/过渡/fix色系，删除purple，统一accent
  - 补丁3: 每Phase标注复用资产矩阵(6组件+6Composable)
  - 补丁4: 工作量修正为22-29h(从14-21h)，加回滚预案和执行优先级
- 新增文件: PipelineView.vue (Phase 2.5 流程管理页)

---

## 前端全面重构 — Phase 1 全局布局审核

### 审核结论: **⚠️ CONDITIONAL PASS (8/10 通过)**

### 核心成果
- :root CSS 变量全量重写完成（旧变量名零残留、purple 色系完全清除）
- 6 个组件样式同步更新（StatusBadge/EmptyState/ConfirmDialog/SearchHeader/ReviewBar/DualPanelLayout），全部带 fallback 安全值
- 侧边栏 hover 展开 56→200px ✅ / 顶部栏 48px (Logo+面包屑) ✅ / 响应式 ≤768px 隐藏侧边栏 ✅
- STATUS_COLOR_MAP 无 purple 引用，三元组格式对齐
- 构建: npm run build 986ms 通过

### 🔴 P0 遗漏: 视图旧变量名未迁移
- 新 :root 删除了旧变量(--line/--panel/--bg/--muted/--chip/--text)，但 **11个视图 ~432 处**仍在引用
- 会导致边框消失/背景透明/文字变默认色的视觉退化
- 修复映射: --line→--border, --panel→--bg-surface, --bg→--bg-muted, --muted→--text-secondary, --text→--text-primary
- 组件不受影响(都有 fallback 值)，仅视图 scoped 样式受影响

### 🟡 P1 小问题
- STATUS_COLOR_MAP 引用的 --*-border 变量在 :root 未定义 → badge 边框不可见
- 视图残留 ~30 处硬编码 hex（低优先级，大部分是 Nave UI props 限制或语义微颜色）

### 报告位置
- `docs/superpowers/reviews/2026-07-01-phase1-audit.md`

---

## 前端全面重构 — Phase 1 复审核（修复后）

### 审核结论: **✅ PASS — 无条件通过 (11/11 全量检查项通过)**

### 修复验证
- **P0-A 旧变量名残留**: 265 处替换 → views/ 下 grep 6 种旧变量模式全部返回 0 ✅
- **P0-B border 变量缺失**: :root 补充 6 个 --xxx-border 定义，StatusBadge 边框可渲染 ✅
- 构建: npm run build 1.14s 通过 ✅

### 技术债清零: 无遗留问题

### 报告位置
- `docs/superpowers/reviews/2026-07-01-phase1-reaudit.md`
- **下一步**: 可继续推进 Phase 2(Dashboard) 或 Phase 2.5(流程管理页面)

---

## 前端全面重构 — Phase 2 + Phase 2.5 审核

### 审核结论: **✅ PASS ×2 — 无条件通过（双 Phase 均无技术债）**

### Phase 2 Dashboard 重构
- 完全重写为治理控制台：4 统计卡(文献总数/未读/已隔离/关系) + 5 待办队列(摄入/解析/元数据/分类/去重) + 4 快捷入口 + 类型分布
- 数据全部来自真实 API（getWorks/getIngestPlan/parseStatus/getMetadataExtractions/getClassificationExtractions/getDuplicates）
- 100% 使用新设计变量，零旧变量名残留
- Promise.allSettled 并发加载队列数据

### Phase 2.5 流程管理页面
- 新增 PipelineView.vue (455行): 4 阶段流水线（收件箱→解析→元数据→分类）
- ConfirmDialog 正确集成：4 个批量操作全部走确认弹窗模式，window.confirm grep=0
- 8 个 API 端点全部使用已有接口（无后端改动）
- 操作完成后自动 loadAll() 刷新数量
- 「去审核→」链接正确指向 /metadata 和 /classification
- 路由 /pipeline 已注册（name=pipeline, meta 含 title/icon/group）
- 响应式适配 ≤640px

### 报告位置
- `docs/superpowers/reviews/2026-07-01-phase2-25-audit.md`
- **下一步**: Phase 3(Works) 或 Phase 4(审核页面统一)

---

## 前端全面重构 — Phase 3 + Phase 4 审核

### 审核结论: **✅ PASS ×2 — 无条件通过（Phase 1-4 全部完成）**

### Phase 3 Works 文献库重构
- EmptyState 集成: 列表为空时显示 "没有匹配的文献" (icon=search)
- CSS 变量全量替换: 48 处 var() 均为新命名
- 功能零退化: 筛选器/表格/分页/隔离弹窗全部保留

### Phase 4 审核页面统一（4 个页面）
- MetadataReview: StatusBadge×4 + EmptyState×1, ~84处CSS替换
- ClassificationReview: StatusBadge×4 + EmptyState×1, ~77处CSS替换
- IntakeReview: StatusBadge×2 + EmptyState×2 + ConfirmDialog×1(晋升确认)
- InboxReview: EmptyState×2 + ConfirmDialog×1(摄入确认)

### 跨页面组件集成总矩阵 (Phase 0-4 累计)
| 组件 | 总引用数 | 分布页面数 |
|------|---------|-----------|
| StatusBadge | 21 处 | 6 页 |
| EmptyState | 12 处 | 7 页 |
| ConfirmDialog | 8 个对话框 | 4 页+PipelineView |

### 残留: ~22 处语义微颜色硬编码(hex)，均为按钮/badge/pagination 微调色，不在设计令牌覆盖范围内，可接受
### TD-1~TD-3 已完全消除，TD-4(SearchHeader)/TD-5(ReviewBar) 为 P3 可选项

### 报告位置
- `docs/superpowers/reviews/2026-07-01-phase3-4-audit.md`
- **Phase 1-5 前端全面重构全部交付完毕**（6轮审核迭代全部PASS，零技术债）
  - Phase 5 审核报告: `docs/superpowers/reviews/2026-07-01-phase5-audit.md`
  - 最终统计: 16个视图+6组件+基础设施文件修改, StatusBadge×30/EmptyState×14/ConfirmDialog×10
- **下一步**: 本地模型正在执行流程管理页面增强(方案: `docs/superpowers/plans/2026-07-01-pipeline-enhancement.md`)

---

## 流程管理页面增强 — 审核完成

### 审核结论: ✅ **PASS（有条件通过）**

### 核心成果
- **StageCard.vue** (325行): 可折叠阶段卡片组件，插槽系统(filters/stats/default)，展开动画，响应式
- **SelectableFileList.vue** (296行): 通用可选文件列表，Set 跨页选中保持，全选/反选/取消，分页，EmptyState 集成
- **PipelineView.vue** (660行→从原455行扩展): 4 个阶段全部使用 StageCard+SelectableFileList 重构
  - 摄入: 文件名搜索 + 大小/时间排序
  - 解析: 状态 Tag(待解析/失败/运行中) + 搜索
  - 元数据: 状态 Tag(待审核/已批准/已拒绝) + 搜索，切换自动 reload
  - 分类: 状态 Tag + 模糊度 Tag(高/中/低) + 搜索，模糊度 badge 彩色编码

### 组件复用验证
| 已用组件 | 引用位置 |
|---------|---------|
| ConfirmDialog | 4 个 batch 操作（全局唯一实例）|
| StatusBadge | 解析/元数据/分类阶段的文件行 |
| EmptyState | SelectableFileList 空状态 |
| useFormatUtils | formatSize + formatDate |

### 技术债 (3 项)
| ID | 优先级 | 描述 |
|----|--------|------|
| TD-P1 | 🟡 P2 | 选择性执行 UI 与 API 行为不一致（按钮说"选中N个"，实际 all_pending: true 全量执行），需加降级提示文字 |
| TD-P2 | 🟢 P3 | StageCard max-height 动画硬编码 600px |
| TD-P3 | 🟢 P3 | 数据刷新后旧 selectedIds 未清理 |

### 红线检查: 10/10 全合规（无新依赖/无 TS / 无 Pinia / alert=0 / confirm=0 / build 通过）

### 报告位置
- `docs/superpowers/reviews/2026-07-01-pipeline-enhancement-audit.md`

---

## Pipeline 数据一致性审核（下午）

**问题**: 用户反馈前端流程管理页面所有阶段显示的待处理数都是 0

**数据库实际情况**:
- 收件箱摄入: _inbox/ 无 PDF 文件 → 显示 0 ✅
- 文档解析: 152/152 全部 succeeded, pending=0 → 显示 0 ✅（但缺少"已完成"提示）
- 元数据抽取: **11 条 pending（10 条被隔离，1 条可显示）→ 应显示 1，但用户说显示 0 🔴**
- 分类抽取: 373 条全审核完, pending=0 → 显示 0 ✅

**根因分析**: 最可能原因是后端服务未启动/API 请求失败，导致前端 catch 吞掉异常，count 保持初始值 0

**产出**: `docs/superpowers/reviews/2026-07-01-pipeline-data-audit.md`（完整诊断报告，含修复建议）

---

## Pipeline 数据问题修复确认 + 端口信息补充

**根因**: per_page=200 超过后端限制(最大100) → 422 Unprocessable Content → catch 吞异常 → 全部显示 0
**修复**: per_page: 200 → per_page: 100 ✅ 构建通过

**端口信息**（从代码中确认）:
- 前端开发服务器: **localhost:19528** (`web/vite.config.js:18`)
- 后端 FastAPI: **localhost:19527** (`web/vite.config.js:8`, LITLIB_API_TARGET)
- CORS 白名单: http://localhost:19528 (`api/main.py:17`)
- ⚠️ 文档中未记录这些端口（docs/ 目录无 16528/19527/19528 记录），需补充到 TECHNICAL_OVERVIEW 或 README

**更正**: 端口信息已存在于 `TECHNICAL_OVERVIEW.md` 第22-23行表格中（之前 grep 未匹配到表格格式）

---

## PipelineView 3 个遗留问题修复 (TD-P1/P2/P3)

- **TD-P1 (P2)**: 选择性执行修复 — batchParse/batchMetadata/batchClassify 全部从 `all_pending: true` 改为传 `work_ids: Array.from(selectedIds)`；batchIngest 改为传 `file_ids`
- **TD-P2 (P3)**: StageCard 展开动画 max-height 从硬编码 600px 改为 CSS 变量 `--stage-expand-max`(默认 800px)
- **TD-P3 (P3)**: 新增 `clearAllSelections()` 函数，在每次批量操作成功后统一清理四阶段选中状态
- 构建: ✅ npm run build 1.00s 通过

---

## 网页上传文件功能实现

**用户需求**: Pipeline 页面收件箱区域添加网页上传按钮，支持多选 PDF + 拖拽，上传后自动触发摄入（创建 work + parse_run）

### 改动范围（3 个文件）
1. **`api/routes/ingest.py`** — 新增 `POST /ingest/upload`
   - 接收 multipart 文件列表（UploadFile）
   - 校验仅允许 .pdf，最大 200MB
   - 保存到 `_inbox/` 目录后自动调用 `execute_plan()` 完成摄入
   - 返回 `{ ok, uploaded, ingested, skipped, summary }`

2. **`web/src/api.js`** — 新增 `uploadFiles(files)` 函数
   - 用 FormData 发送 multipart/form-data
   - 不走 request()（避免 Content-Type 被覆盖为 application/json）
   - 自带错误处理和超时（120s）

3. **`web/src/views/PipelineView.vue`** — 收件箱 StageCard 添加 NUpload 组件
   - 使用 Naive UI 内置 NUpload（unplugin 自动导入），支持多选 + accept=.pdf
   - 上传中显示 loading 态，成功后 message.success 提示并 loadAll() 刷新
   - 引入 useMessage 用于用户提示

- 构建: ✅ npm run build 962ms 通过
- **红线合规**: 无新依赖/无 TS /无改现有逻辑结构 / 复用 execute_plan

---

## Pipeline 数据统计逻辑修复

**问题**: 用户反馈 Pipeline 页面数据显示不准确
1. "待抽取"实际显示的是"待审核"数据（metadata_extractions 表中已有记录但 review_status=pending）
2. 漏掉了真正"待抽取"的文件（解析成功但没有 metadata_extraction 记录的 work）

**根因**: 前端 loadMetadata/loadClassify 直接用 `GET /metadata?status=pending`，只查 metadata_extractions 表，查不到不存在的记录

**修复（4 个文件）**:
1. **后端新增** `GET /pipeline/stats` 接口 (`api/routes/ingest.py`)
   - 返回各阶段准确统计，区分两种状态:
     - `pending_extract`: 解析成功但无 extraction 记录 → 真正待抽取
     - `pending_review`: 有 extraction 记录且 review_status=pending → 待审核
   - SQL: `EXISTS(parse_runs succeeded) AND NOT EXISTS(extractions)`
2. **前端 api.js**: 新增 `getPipelineStats()`
3. **PipelineView.vue**: loadMetadata/loadClassify 改用新接口获取 count
4. **StageCard.vue**: 新增 `secondaryCount`/`secondaryLabel` props，支持双 badge 显示

- **修复后数据**: 元数据阶段=待抽取11 + 待审核1（含 Monitoring Monitorability）
- InboxReview.vue: 修复 `<table>` 前空行导致的 Vite HTML 警告
- python-multipart 依赖安装（UploadFile 需要）
- 构建: ✅ 1.07s 通过

---

## Pipeline 全阶段"待抽取"标签缺失修复

**问题**（用户截图反馈）:
1. 元数据/分类阶段的标签栏只有「待审核/已批准/已拒绝/全部」，**缺少最关键的"待抽取"选项**
2. "待抽取"的文件（解析成功但无 extraction 记录）在 `metadata_extractions` 表里根本不存在，现有 API 无法查出
3. 默认选中"待审核"而非"待抽取"，用户最需要的功能被隐藏
4. 分类阶段有完全相同的问题

**修复（5 个文件全面改动）**:

### 后端 `api/routes/ingest.py` — 新增 2 个列表接口
- **`GET /pipeline/pending-metadata`**: 返回待元数据抽取的 works 列表
  - SQL: `works JOIN parse_runs(succeeded) AND NOT EXISTS(metadata_extractions)`
  - 支持 per_page + page 分页，返回 `{total, items: [{work_id, title, created_at, parse_status}]}`
- **`GET /pipeline/pending-classification`**: 返回待分类抽取的 works 列表
  - SQL: `works JOIN parse_runs(succeeded) JOIN metadata_extractions(approved) AND NOT EXISTS(classification_extractions)`
  - 返回 `{total, items: [{work_id, title, created_at, meta_model}]}`

### 前端 `web/src/api.js` — 新增 2 个调用函数
- `getPendingMetadataWorks(params)` → `/pipeline/pending-metadata`
- `getPendingClassifyWorks(params)` → `/pipeline/pending-classification`

### 前端 `PipelineView.vue` — 核心逻辑重写
1. **标签数组** metadataStatuses/classifyStatuses 首位加入 `{key:'extract', label:'待抽取'}`
2. **默认选中** statusFilter 从 `'pending'` 改为 `'extract'`
3. **loadMetadata/loadClassify** 根据 statusFilter 分流:
   - `=== 'extract'`: 调用新接口获取 works 列表，统一映射 `{id:work_id, display_title:title, _source:'extract'}`
   - 否则: 调用原审核接口获取 extractions 记录，映射 `{id:work_id, display_title:work_title, _source:'review'}`
4. **模板双模式渲染**:
   - extract 模式: 显示 `StatusBadge(pending)` + 标题 + "解析完成"/"元数据已批准" + 日期
   - review 模式: 显示 `StatusBadge(status)` + 标题 + model_name/模糊度badge + 日期
5. **筛选计算属性** 统一使用 `display_title` 字段搜索

### 数据验证
- **元数据待抽取: 11 篇** ✅（含刚上传的3篇 + 之前8篇）
- **分类待抽取: 0 篇** ✅（尚无元数据被批准）
- 构建: ✅ npm run build 942ms 通过

---

## Pipeline 审核模式 UX 修复（"待审核"文件不应能再"抽取"）

**问题**: 用户反馈已抽取完毕（待审核状态）的文件仍可勾选复选框 + 点击"抽取选中"按钮，逻辑矛盾

**根因**: SelectableFileList 无条件显示复选框，StageCard 执行按钮只检查 selectedCount > 0，未区分当前是"待抽取"还是"待审核"模式

**修复（3 个文件）**:

### 1. SelectableFileList.vue — 新增 `selectable` prop
- 默认 `true`（向后兼容）
- 当 `selectable=false` 时：
  - 隐藏工具栏（全选/反选/取消全选）
  - 隐藏每行 checkbox
  - 行点击不再触发 toggleItem
  - 列表变为纯展示模式（适合审核列表）

### 2. StageCard.vue — 新增 `actionDisabled` prop
- 执行按钮 disabled 条件增加 `|| actionDisabled`
- 禁用时 hover 显示 tooltip: "当前为审核模式，请切换到「待抽取」标签后操作"

### 3. PipelineView.vue — 按模式控制
- 元数据/分类阶段的 SelectableFileList: `:selectable="statusFilter === 'extract'"`
- 元数据/分类阶段的 StageCard: `:action-disabled="statusFilter !== 'extract'"`
- 切换标签时自动清空选中项 (`selectedIds = new Set()`)

**效果**:
| 模式 | 复选框 | 执行按钮 | 去审核链接 |
|------|--------|---------|-----------|
| **待抽取** (extract) | ✅ 可选 | ✅ 可点 → 触发 N 个文件的抽取 | ✅ 显示 |
| **待审核** (pending) | ❌ 隐藏 | ❌ 禁用 + 提示 | ✅ 显示 |
| 已批准/已拒绝 | ❌ 隐藏 | ❌ 禁用 + 提示 | ✅ 显示 |

- 构建: ✅ npm run build 925ms 通过

---

## 全部 4 阶段 selectable/actionDisabled 同步修复

**问题**: 元数据/分类阶段已修复，但解析阶段有同样 bug（切换到"失败"/"运行中"标签仍可勾选 + 点"解析选中"）

**修复范围**: 解析阶段同步加防护（收件箱摄入无需改——只有单模式，所有文件均可摄入）

### 解析阶段改动
1. **标签顺序**: `待解析` 排第一（默认选中），`全部`/`失败`/`运行中` 后排
2. **默认 statusFilter**: `''`(全部) → `'pending'`（待解析）
3. **SelectbleFileList**: 加 `:selectable="statusFilter === 'pending'"` — 非 pending 标签隐藏复选框
4. **StageCard**: 加 `:action-disabled="statusFilter !== 'pending'"` — 非 pending 禁用按钮
5. **切标签自动清空选中项**

### 最终全阶段状态矩阵
| 阶段 | 可操作标签 | 审核模式 | 收件箱(无标签) |
|------|-----------|---------|--------------|
| **收件箱摄入** | ✅ 始终可操作 | — | ✅ 始终可操作 |
| **文档解析** | 仅「待解析」可操作 | ❌ 复选框隐藏+按钮禁用 | — |
| **元数据抽取** | 仅「待抽取」可操作 | ❌ 已修 | — |
| **分类抽取** | 仅「待抽取」可操作 | ❌ 已修 | — |

- 构建: ✅ npm run build 950ms 通过

---

## Pipeline 文件预览 + 快速隔离功能

**用户需求**: 待抽取列表只能看到文件名（如 1706.03762），无法判断内容；需要预览功能；不相关的文献需要能在流程管理页直接隔离

**设计方案**:
- 每个文件行加「预览」按钮 → 点击右侧弹出 PdfPreviewDrawer（复用现有组件）
- 抽屉底部有「查看详情 →」链接和「隔离此文献」按钮
- 待抽取模式的行额外显示「隔离」小按钮
- 隔离弹窗复用 useQuarantine composable（type='work'，6 种原因可选）

**改动文件**: `PipelineView.vue` 单文件（~870行，从 807 行扩展）

### 新增能力
| 位置 | 功能 | 复用资产 |
|------|------|---------|
| 收件箱/解析/元数据/分类 所有行 | 「预览」按钮 | usePdfDrawer |
| 待抽取模式行（元数据/分类/解析）| 行内「隔离」按钮 | useQuarantine(type=work) |
| 页面底部 | PdfPreviewDrawer 抽屉 | defineAsyncComponent 动态加载 |
| 抽屉底部操作栏 | 「查看详情→」+「隔离此文献」 | router-link + openQuarantine |
| 页面底部 | 隔离选择弹窗（6 种原因） | useQuarantine 完整 UI |

### 数据流
1. 用户点「预览」→ `handlePreview(workId, title)` → `openPdf(workId)` → PdfPreviewDrawer 打开
2. 抽屉内看 PDF 内容后判断：
   - 正常 → 关闭抽屉，勾选该文件，点"抽取选中"
   - 不相关 → 点抽屉底部的"隔离此文献"或行的"隔离"→ 弹出原因选择 → 确认 → 自动 refresh 列表

- 构建: ✅ npm run build 920ms 通过（PdfPreviewDrawer 单独 chunk: 126KB gzipped）

---

## Pipeline PDF 预览 url 缺失修复

**问题**: 点击「预览」按钮后控制台报警告 `Missing required prop: "url"`，页面无反应
**根因**: `PdfPreviewDrawer` 组件需要两个必需 props（`url` + `workId`），但 PipelineView 只传了 `workId`，漏传 `url`
**修复** (PipelineView.vue，4 处改动):
1. import 新增 `pdfUrl` from api.js
2. 新增 `activePdfUrl` ref 存储当前 PDF 地址
3. `handlePreview()` 中调用 `pdfUrl(workId)` 设置地址
4. PdfPreviewDrawer 组件加 `:url="activePdfUrl"` prop
**验证**: 刷新后点预览应正常打开右侧 PDF 抽屉

---

## Pipeline 页面布局重构 — PDF 抽屉与流程并列显示

**问题**: PDF 预览抽屉接在流程阶段底部（分类抽取之后），而不是和流程内容左右并列
**修复** (PipelineView.vue):
1. 外层容器从 `.pipeline` 改为 `.pipeline-layout`（flex 横向双栏）
2. 新增 `.pipeline-main` 包裹所有 StageCard（左侧，可滚动）
3. PdfPreviewDrawer 移到 pipeline-main 外部作为右侧独立栏
4. 有 PDF 时左侧占 55% 宽度，无 PDF 时撑满
5. ConfirmDialog 和隔离弹窗保持在最外层（覆盖层不影响布局）
6. 构建通过: 1.05s

---

## 文档归档 — PROJECT_HISTORY.md + FUTURE_WORK_PLAN.md 更新

### PROJECT_HISTORY.md 更新
- 新增「前端全面重构 Phase 0-5」章节（6轮审核PASS，7 Composable + 6 UI 组件）
- 新增「流程管理页面创建与增强」章节（PipelineView/StageCard/SelectableFileList/上传/预览/隔离）
- 新增「V1.3 知识闭环 P0-P2」章节
- 复盘参考列表按阶段分组扩展

### FUTURE_WORK_PLAN.md 清理
- V1.3 标记为 ✅ 已完成，从"当前近期重点"移至已完成区域
- 「流程管理面板(批量触发)」标记为 ✅ 已完成
- 新增 🔴 IngestHub 文献入库统一页面为最高优先级任务

---

## IngestHub 文献入库统一页面 — 实施

**用户需求**: 发现检索和收件箱是两个并列的入库入口，应合并为一个 Tab 页面

### 改动清单（4 个文件）
1. **新建 `IngestHub.vue`** (55行) — NTabs 薄壳包装 DiscoveryReview + InboxReview
2. **`router.js`** — 新增 `/ingest` 路由
3. **`AppLayout.vue`** — 侧边栏「发现检索」+「收件箱」→ 替换为「📥 文献入库」（active 匹配 3 条路径）
4. **`PipelineView.vue`** — 收件箱 StageCard 移除上传按钮 → 改为跳转链接；清理 uploadFiles/useMessage/message/uploading/handleFileListChange 等无用代码
- 构建: ✅ 通过 (21.17s)，IngestHub 独立 chunk 9KB gzipped
