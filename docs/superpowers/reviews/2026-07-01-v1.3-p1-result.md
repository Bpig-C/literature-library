# V1.3 P1 阶段审核结果

> 审核日期：2026-07-01
> 完成 commit：`2a30c0f`（含 P1-1/P1-2/P1-3 + 修复）

---

## 一、P1-1: 发现检索页加「关联主题」下拉

### 变更摘要
- `DiscoveryReview.vue` 添加 `getIntakeTopics` 导入和 `topics` ref
- 把「Topic ID」自由文本框换成 `<select>` 下拉
- `doCompositePlan` 在 `discoveryRun` 调用中传递 `topic_id`
- 新增 `test_composite_run_with_topic_inherits_to_candidate` 测试

### 审核结论：✅ 通过

---

## 二、P1-2: 建主题时选分类标签（含 proposed_new）

### 变更摘要
- `collector/topics.py` 的 `create()` 新增 `mapped_tags` 参数
- 按词表自动判定 status（approved/proposed_new）
- `TopicCreateBody` 新增 `mapped_tags` 字段
- `TopicsReview.vue` 添加标签选择区

### 审核结论：✅ 通过

---

## 三、P1-3: query_def 复合字段扩展

### 变更摘要
- `topics.create` 新增 `query_def` 参数
- 新增 `topics.update_query_def` + PATCH 端点
- `draft_composite_plan` 读 topic.query_def 作底值
- `TopicsReview.vue` 添加可折叠「高级线索」区
- 更新 `docs/discovery-agent-protocol.md`

### 审核结论：✅ 通过

---

## 四、验收结果

| 验收命令 | 结果 |
|---------|------|
| 后端 discovery 套件 (166 tests) | ✅ 166 passed, 0 failed |
| 全量回归 (509 tests) | ✅ 509 passed, 7 skipped, 0 failed |
| 前端构建 (npm run build) | ✅ built in 780ms |
| 健康检查 | ✅ 五项全零 |

---

## 五、P1 阶段提交记录

| Commit | 说明 |
|--------|------|
| `e51f483` | feat(web): composite form topic selector + pass topic_id to run (P1-1) |
| `b30ff7f` | feat(topics): create with mapped_tags + proposed_new custom values (P1-2) |
| `476d6a2` | feat(topics): rich query_def + composite-plan reads topic clues (P1-3) |
| `2a30c0f` | fix(topics): createForm reset tags to [] instead of '' (P1-3 review fix) |

---

> **P1 阶段完成，可以进入 P2 阶段实施。**
