# V1.3 P0 阶段审核结果

> 审核日期：2026-07-01
> 基线 commit：`b56ee03`
> 完成 commit：`7e4752b`（P0-2）/ `4fcfe21`（P0-1）

---

## 一、P0-1: 发现运行状态机修复 + /complete 端点

### 变更摘要

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `collector/discovery.py` | 新增函数 | `complete_run()` — 只接受 succeeded/failed 终态 |
| `collector/discovery.py` | 修改 | `insert_discovery_hit` 的 run UPDATE 加 CASE WHEN flip planned→running |
| `api/routes/discovery.py` | 新增端点 | `POST /discovery/runs/{run_id}/complete` |
| `api/routes/discovery.py` | 新增模型 | `CompleteBody(BaseModel)` — status: Literal["succeeded", "failed"] |
| `api/routes/discovery.py` | 修改 | `backfill_hits` 自动翻转收窄为 "any created → succeeded" |
| `tests/test_discovery_core.py` | 新增测试 | `test_insert_hit_flips_planned_to_running`, `test_complete_run_only_accepts_terminal_status` |
| `tests/test_discovery_api.py` | 新增测试 | `test_complete_run_endpoint`, `test_backfill_marks_succeeded_when_created` |
| `docs/discovery-agent-protocol.md` | 更新 | agent 完成回填后零命中/放弃应调 /complete |

### 测试覆盖
- `test_insert_hit_flips_planned_to_running`: 第一条 hit 回填后 run.status 从 planned → running ✅
- `test_complete_run_only_accepts_terminal_status`: complete_run 只接受 succeeded/failed，拒绝 planned/running ✅
- `test_complete_run_endpoint`: POST /complete 标记 succeeded；非法 status 返 400；不存在返 404 ✅
- `test_backfill_marks_succeeded_when_created`: backfill 有 created hit 后 run 必须 succeeded ✅

---

## 二、P0-2: agent 回填命中时即时查 works 重

### 变更摘要

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `collector/discovery.py` | 新增函数 | `_check_works_dup()` — metadata-only 通过 light_gate 查重 |
| `collector/discovery.py` | 修改 | `insert_discovery_hit` 在 INSERT 前调用 `_check_works_dup` |
| `api/routes/discovery.py` | 修改 | `HitBackfillItem` 新增 `doi`/`arxiv_id` 字段 |
| `tests/test_discovery_core.py` | 新增测试 | `test_hit_dup_of_existing_work_marked`, `test_hit_new_when_no_work_match`, `test_hit_dup_does_not_block_insert` |
| `tests/test_discovery_api.py` | 新增测试 | `test_backfill_dup_by_arxiv_id_marked`, `test_backfill_dup_by_doi_marked` |
| `docs/discovery-agent-protocol.md` | 更新 | 回填字段表加 doi/arxiv_id + dup_of_works 说明 |

### 测试覆盖
- `test_hit_dup_of_existing_work_marked`: title 与 works 中已有文献高度相似 → 自动带 dup_of_works ✅
- `test_hit_new_when_no_work_match`: 全新标题 → verification_status 保持 unverified ✅
- `test_hit_dup_does_not_block_insert`: 即使是重复，也必须插入（status=created），不阻止 ✅
- `test_backfill_dup_by_arxiv_id_marked`: agent 回填带 arxiv_id 且 works 已有 → hit 标 dup_of_works ✅
- `test_backfill_dup_by_doi_marked`: agent 回填带 doi 且 works 已有 → hit 标 dup_of_works ✅

---

## 三、验收结果

| 验收命令 | 结果 |
|---------|------|
| 后端 discovery 套件 (137 tests) | ✅ 137 passed, 0 failed |
| 前端构建 (npm run build) | ✅ built in 13.94s |
| 健康检查 (healthcheck_library.py) | ✅ 五项全零 |

---

## 四、反偷懒自检

- [x] 每个新函数/端点都有对应的失败测试先跑红过
- [x] `verification_status="dup_of_works"` 没阻止插入（重复也 created）
- [x] 三行验收命令全过
- [x] healthcheck 五项全零
- [x] 不留 TODO/TBD/「适当处理」/「类似上文」

---

## 五、遗留问题

1. P0 阶段不涉及前端改动，P1-1 的 discoveryRun topic_id 传递留到 P1 实施
2. `_check_works_dup` 中的 `normalize_arxiv_id` 调用在 URL 不是 arXiv 时返回 None，这是预期行为

---

> **P0 阶段完成，可以进入 P1 阶段实施。**
