# V1.3 知识闭环（主题·发现·收件箱关联改造）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按任务逐条实施。步骤用 `- [ ]` 复选框跟踪。
> **本文档是唯一权威实施依据。** 代码位置、函数名、SQL、行号均已对齐当前 master 真实状态（基线：`b56ee03`）。凡本文档给出的代码块均为可直接落地的完整内容，不是伪代码。

**Goal:** 打通「主题闸门 ↔ 发现检索 ↔ 收件箱」三者关联，让每一轮检索都能继承主题线索、即时查重、回填后状态正确，形成持续迭代的知识闭环。

**Architecture:** 后端在 `collector/discovery.py` + `collector/topics.py` + `collector/gate.py` 内补齐状态机与查重；API 层（`api/routes/discovery.py`、`api/routes/intake.py`）扩展 Body 与端点；前端在 `web/src/views/DiscoveryReview.vue`、`TopicsReview.vue`、`components/AppLayout.vue` 内补齐交互。不改任何表结构（`discovery_runs.status` / `discovery_hits.verification_status` / `collection_topics.query_def` 均为 TEXT，直接写新值即可）。

**Tech Stack:** Python 3 + FastAPI + Pydantic + SQLite（`api/db.py`）；Vue 3 `<script setup>` + Vite（无 Pinia/Vuex，状态为本地 ref）；naive-ui 已装但审核页用原生 HTML。

---

## 一、背景与目标

### 1.1 当前状态（基线 `b56ee03`）

| 项目 | 数值 |
|------|:----:|
| 正式文献 works | 146 |
| 收件箱候选 intake_candidates | 4 |
| 发现运行 discovery_runs | 2（1 planned + 5 hits，1 succeeded） |
| 发现命中 discovery_hits | 5（全 pending） |
| 主题 collection_topics | 1（seedling） |
| 分类标签组 | 4 组（risk_domain / artifact_focus / method_tags / reading_lane） |

### 1.2 核心问题：三者割裂

主题闸门、发现检索、收件箱各自独立，回填命中不关联回主题、发现页无法选主题、主题线索字段单薄、运行状态不更新、回填时不查 works 重。

### 1.3 目标闭环

```
建主题(选领域+填线索) → 发现检索(agent执行+即时查重)
     │              用户审核 accept
     │            收件箱闸门(再次查重+分类)
     │              入库 → 元数据抽取 → 分类打标 → 审核通过
     └──────────── 反哺主题线索 ◄───────────────────────
```

---

## 二、文件结构总览（改动锁定）

| 文件 | 职责 | 本计划涉及任务 |
|------|------|----------------|
| `collector/discovery.py` | run/hit 核心；状态机；查重入口 | P0-1, P0-2, P1-1, P1-3 |
| `collector/gate.py` | `light_gate` 元数据查重（被 P0-2 复用，不改） | P0-2（只读引用） |
| `collector/topics.py` | 主题 CRUD；`create`/`get`/新增 `update_query_def` | P1-2, P1-3 |
| `api/routes/discovery.py` | discovery 路由与 Body 模型 | P0-1, P0-2, P1-1, P1-3 |
| `api/routes/intake.py` | `/intake/topics/create` 等 | P1-2, P1-3 |
| `api/classification_vocab.py` | 词表（静态 dict，不改结构） | P1-2（只读） |
| `web/src/views/DiscoveryReview.vue` | 发现检索页 | P1-1, P2-2 |
| `web/src/views/TopicsReview.vue` | 主题闸门页 | P1-2, P1-3, P2-2 |
| `web/src/components/AppLayout.vue` | 侧边栏 + 全局 CSS 变量 | P2-1 |
| `web/src/api.js` | 前端 API 封装 | P0-1, P1-2, P1-3 |
| `docs/discovery-agent-protocol.md` | agent 执行规范 | P0-1, P0-2, P1-3 |
| `tests/test_discovery_core.py` | 核心逻辑测试 | 全部后端任务 |
| `tests/test_discovery_api.py` | API 测试 | 全部后端任务 |

**纪律：每个任务独立可测、独立提交。先写失败测试 → 跑红 → 写最小实现 → 跑绿 → commit。**

---

## 三、任务清单（P0 → P2，逐条 TDD）

### 任务 P0-1：发现运行状态机修复（planned → running → succeeded/failed）

**根因（已核对真实代码）：**
- `collector/discovery.py:552-556` 的 `insert_discovery_hit()` 只 `UPDATE discovery_runs SET hits_created = hits_created + 1`，**从不写 status**。
- `api/routes/discovery.py:256-258` 的 `backfill_hits` 端点在 `created>0` 时把 `planned` 直接翻成 `succeeded`——**`running` 在全代码库没有任何路径可达**（`VALID_RUN_STATUSES` 含 `running` 但无人写）。
- **无** `/discovery/runs/{run_id}/complete` 端点，agent 无法标记失败或零命中收尾。

**目标行为：**
1. `insert_discovery_hit` 写入第一条 hit 时把 run 从 `planned` 翻成 `running`（有 hit 就绝不停在 planned）。
2. 新增 collector `complete_run()` + API `POST /discovery/runs/{run_id}/complete`，允许 agent/用户标记 `succeeded`/`failed`。
3. `backfill_hits` 的自动翻转收窄为「有 created 即 succeeded」（覆盖 planned 与 running 两种快照）。

**Files:**
- Modify: `collector/discovery.py:485-560`（`insert_discovery_hit`）、`collector/discovery.py:463-478`（`update_run_status` 后新增 `complete_run`）
- Modify: `api/routes/discovery.py:238-266`（`backfill_hits`）、新增 `complete_run` 端点与 `CompleteBody`
- Test: `tests/test_discovery_core.py`、`tests/test_discovery_api.py`

- [ ] **Step 1：写失败测试（core）**

在 `tests/test_discovery_core.py` 末尾追加：

```python
def test_insert_hit_flips_planned_to_running(hermetic_db, sample_run):
    """P0-1: 第一条 hit 回填后，run.status 必须从 planned 变为 running。"""
    from collector import discovery
    run = discovery.create_discovery_run(
        mode="composite",
        input_json={"mode": "composite"},
        search_plan_json={"queries": ["q"]},
        executor="agent:web-access",
    )
    assert run["status"] == "planned"

    discovery.insert_discovery_hit(
        run_id=run["id"], url="https://arxiv.org/abs/2501.99999",
        title="A Test Paper", query="q",
    )
    refreshed = discovery.get_discovery_run(run["id"])
    assert refreshed["status"] == "running", refreshed["status"]
    assert refreshed["hits_created"] == 1


def test_complete_run_only_accepts_terminal_status(hermetic_db, sample_run):
    """P0-1: complete_run 只接受 succeeded/failed，拒绝 planned/running。"""
    from collector import discovery
    run = discovery.create_discovery_run(
        mode="composite", input_json={}, search_plan_json={},
        executor="agent:web-access")
    discovery.complete_run(run["id"], status="succeeded")
    assert discovery.get_discovery_run(run["id"])["status"] == "succeeded"

    import pytest
    with pytest.raises(ValueError):
        discovery.complete_run(run["id"], status="running")
    with pytest.raises(KeyError):
        discovery.complete_run("DR-does-not-exist", status="failed")
```

> 注：`hermetic_db` / `sample_run` 是该测试文件已有的 fixture；若名字不同，照搬文件里已有的那个。先 `grep -n "def test_" tests/test_discovery_core.py` 看现有写法对齐 fixture 名。

- [ ] **Step 2：跑红**

```bash
uv run python -m pytest tests/test_discovery_core.py::test_insert_hit_flips_planned_to_running tests/test_discovery_core.py::test_complete_run_only_accepts_terminal_status -q
```
预期：FAIL（`complete_run` 不存在 → AttributeError；status 仍是 planned）。

- [ ] **Step 3：改 `insert_discovery_hit` 的 run UPDATE（`collector/discovery.py:552-556`）**

把这段：

```python
        # Update run's hits_created count
        conn.execute(
            "UPDATE discovery_runs SET hits_created = hits_created + 1, updated_at=? WHERE id=?",
            (now, run_id),
        )
```

替换为：

```python
        # Bump hits_created AND flip planned→running on first hit (P0-1).
        # running is the "has produced evidence" state; terminal succeeded/failed
        # is set by backfill auto-flip or POST /runs/{id}/complete.
        conn.execute(
            """UPDATE discovery_runs
               SET hits_created = hits_created + 1,
                   status = CASE WHEN status='planned' THEN 'running' ELSE status END,
                   updated_at=?
               WHERE id=?""",
            (now, run_id),
        )
```

- [ ] **Step 4：新增 `complete_run`（`collector/discovery.py`，紧接 `update_run_status` 函数之后，约 479 行后插入）**

```python
def complete_run(run_id: str, *, status: str, error: str | None = None) -> None:
    """Mark a run terminal. status must be 'succeeded' or 'failed'.

    Distinct from update_run_status: only terminal states are allowed here,
    so callers can't accidentally rewind a run to 'planned'/'running'.
    Raises ValueError on bad status, KeyError if run missing.
    """
    if status not in ("succeeded", "failed"):
        raise ValueError(f"complete status must be 'succeeded' or 'failed', got {status!r}")
    update_run_status(run_id, status=status, error=error)
```

- [ ] **Step 5：跑 core 测试到绿**

```bash
uv run python -m pytest tests/test_discovery_core.py -q
```
预期：PASS（含两条新测试）。

- [ ] **Step 6：写失败测试（api）**

在 `tests/test_discovery_api.py` 追加：

```python
def test_complete_run_endpoint(client, sample_run_id):
    """P0-1: POST /discovery/runs/{id}/complete 标记 succeeded；非法 status 返 400。"""
    r = client.post(f"/api/discovery/runs/{sample_run_id}/complete",
                    json={"status": "succeeded"})
    assert r.status_code == 200
    assert r.json()["status"] == "succeeded"

    bad = client.post(f"/api/discovery/runs/{sample_run_id}/complete",
                      json={"status": "running"})
    assert bad.status_code == 400

    missing = client.post("/api/discovery/runs/DR-nope/complete",
                          json={"status": "failed"})
    assert missing.status_code == 404


def test_backfill_marks_succeeded_when_created(client, sample_run_id):
    """P0-1: backfill 有 created hit 后 run 必须 succeeded（无论起点 planned 还是 running）。"""
    r = client.post(f"/api/discovery/runs/{sample_run_id}/hits",
                    json=[{"url": "https://arxiv.org/abs/2501.88888",
                           "title": "Backfill Paper", "query": "q"}])
    assert r.status_code == 200
    assert r.json()["created"] == 1
    run = client.get(f"/api/discovery/runs/{sample_run_id}").json()
    assert run["status"] == "succeeded"
```

> `sample_run_id` fixture 名照搬该文件现有写法；若没有，建一个 planned run 并返回其 id。

- [ ] **Step 7：跑红**

```bash
uv run python -m pytest tests/test_discovery_api.py::test_complete_run_endpoint tests/test_discovery_api.py::test_backfill_marks_succeeded_when_created -q
```
预期：FAIL（404 路由不存在 / 旧逻辑翻转条件不对）。

- [ ] **Step 8：加 `CompleteBody` 与端点（`api/routes/discovery.py`）**

文件顶部 import 区加（若尚无）：

```python
from typing import Literal
```

在 `RejectBody` / `BatchAcceptBody` 附近（约 65 行后）新增：

```python
class CompleteBody(BaseModel):
    status: Literal["succeeded", "failed"]
    error: str | None = None
```

在 `backfill_hits` 之后（约 267 行后）新增端点：

```python
@router.post("/discovery/runs/{run_id}/complete")
def complete_run(run_id: str, body: CompleteBody):
    """Mark a discovery run terminal (succeeded/failed).

    Use after agent backfill to signal done, or to record a run that yielded
    no hits / was aborted. Hits already inserted are preserved.
    """
    try:
        discovery.complete_run(run_id, status=body.status, error=body.error)
    except KeyError as e:
        raise HTTPException(404, str(e))
    except ValueError as e:
        raise HTTPException(400, str(e))
    return {"ok": True, "run_id": run_id, "status": body.status}
```

- [ ] **Step 9：收窄 `backfill_hits` 自动翻转（`api/routes/discovery.py:256-258`）**

把这段：

```python
    # Update run status to succeeded if it was planned
    if run["status"] == "planned" and created > 0:
        discovery.update_run_status(run_id, status="succeeded")
```

替换为：

```python
    # Any created hit ⇒ run produced results ⇒ mark succeeded (P0-1).
    # Covers both 'planned' (stale single-call) and 'running' (insert already
    # flipped it). Zero-hit backfills leave status alone; agent calls /complete.
    if created > 0:
        discovery.update_run_status(run_id, status="succeeded")
```

- [ ] **Step 10：跑 api 测试到绿，并跑全 discovery 套件回归**

```bash
uv run python -m pytest tests/test_discovery_core.py tests/test_discovery_cli.py tests/test_discovery_api.py tests/test_discovery_smoke.py -q
```
预期：全 PASS（不得低于基线通过数）。

- [ ] **Step 11：更新 agent 协议文档**

`docs/discovery-agent-protocol.md` 在「回填」一节补一句：agent 完成回填后若**零命中**或**主动放弃**，应调用 `POST /api/discovery/runs/{run_id}/complete`（`{"status":"failed","error":"..."}`）；正常回填有命中时后端自动置 `succeeded`，无需再调。

- [ ] **Step 12：commit**

```bash
git add collector/discovery.py api/routes/discovery.py tests/test_discovery_core.py tests/test_discovery_api.py docs/discovery-agent-protocol.md
git commit -m "fix(discovery): run status machine (planned→running→succeeded/failed) + /complete endpoint (P0-1)"
```

---

### 任务 P0-2：agent 回填命中时即时查 works 重（不阻止，标记 dup_of_works）

**根因（已核对真实代码）：**
- `insert_discovery_hit()` 只做 run 内去重（`run_id + dedup_key`，`collector/discovery.py:527-533`），**不查 works 表**。
- `collector/gate.py:47` 的 `light_gate(candidate: dict)` 是现成的元数据查重：读 `arxiv_id`/`doi`/`title`，返回 `(resolution, matched_work_id)`，resolution ∈ {`exact_hit`,`needs_better_copy`,`title_candidate`,`new`}。**不下载，毫秒级。**
- **关键约束：works 表没有 `url_canonical` 列**（只有 `arxiv_id`/`doi`/`title`）。所以不能按 URL 查 works，只能按 arxiv_id/doi（强键）+ title（Jaccard≥0.9）。
- `HitBackfillItem`（`api/routes/discovery.py:40-52`）当前**没有** `doi`/`arxiv_id` 字段；`insert_discovery_hit` 本身已接受 `doi`/`arxiv_id` 形参。

**目标行为：**
1. `HitBackfillItem` 增加 `doi`/`arxiv_id` 字段，让 agent 能回填强键。
2. `insert_discovery_hit` 在 INSERT 前调用 `light_gate`：resolution ≠ `new` 即视为重复 → `verification_status="dup_of_works"`，并把 `matched_work_id` 写进 `raw_json["dup_of_work_id"]`。
3. **绝不阻止插入**（agent 需要知道这个事实；用户审核时跳过或降级）。
4. 异常安全：查重失败时按 `unverified` 落库，不抛。

**Files:**
- Modify: `collector/discovery.py:485-560`（新增 `_check_works_dup` + 接入 `insert_discovery_hit`）
- Modify: `api/routes/discovery.py:40-52`（`HitBackfillItem` 加字段）
- Test: `tests/test_discovery_core.py`、`tests/test_discovery_api.py`

- [ ] **Step 1：写失败测试（core）**

```python
def test_hit_dup_of_existing_work_marked(hermetic_db, sample_work, sample_run):
    """P0-2: 回填一条 title 与 works 中已有文献高度相似的 hit → 自动带 dup_of_works。"""
    import json
    from collector import discovery
    # sample_work 已在 works 表里（fixture 插入），title 设为 "Goal Misgeneralization Survey"
    run = discovery.create_discovery_run(
        mode="composite", input_json={}, search_plan_json={},
        executor="agent:web-access")
    res = discovery.insert_discovery_hit(
        run_id=run["id"], title="Goal Misgeneralization Survey",
        url="https://example.org/some-blog", query="q",
    )
    assert res["status"] == "created"
    hit = discovery.list_hits({"run_id": run["id"]})[0]
    assert hit["verification_status"] == "dup_of_works"
    raw = json.loads(hit["raw_json"]) if hit["raw_json"] else {}
    assert raw.get("dup_of_work_id") == sample_work["id"]


def test_hit_new_when_no_work_match(hermic_db, sample_run):
    """P0-2: 全新标题 → verification_status 保持 unverified。"""
    from collector import discovery
    run = discovery.create_discovery_run(
        mode="composite", input_json={}, search_plan_json={},
        executor="agent:web-access")
    discovery.insert_discovery_hit(
        run_id=run["id"], title="A Brand New Unrelated Paper XYZ123",
        url="https://example.org/new", query="q")
    hit = discovery.list_hits({"run_id": run["id"]})[0]
    assert hit["verification_status"] == "unverified"


def test_hit_dup_does_not_block_insert(hermetic_db, sample_work):
    """P0-2: 即使是重复，也必须插入（status=created），不阻止。"""
    from collector import discovery
    run = discovery.create_discovery_run(
        mode="composite", input_json={}, search_plan_json={},
        executor="agent:web-access")
    res = discovery.insert_discovery_hit(
        run_id=run["id"], title=sample_work["title"], url="https://x.org/y")
    assert res["status"] == "created"
```

> `sample_work` fixture 若不存在，在该测试文件内新建一个 helper：往 works 表插一行 `{id:"WK-test", title:"Goal Misgeneralization Survey", arxiv_id:"", doi:""}`。先 grep 现有 fixture 复用。

- [ ] **Step 2：跑红**

```bash
uv run python -m pytest tests/test_discovery_core.py -k "dup_of_existing_work or new_when_no_work or does_not_block" -q
```
预期：FAIL（verification_status 仍是 unverified）。

- [ ] **Step 3：新增 `_check_works_dup`（`collector/discovery.py`，置于 `insert_discovery_hit` 之前）**

```python
def _check_works_dup(*, title: str = "", doi: str = "", arxiv_id: str = "",
                     url: str = "") -> str | None:
    """Return matched work_id if this hit likely duplicates an existing work, else None.

    Metadata-only via gate.light_gate (no download, ms-level). Tries to derive
    an arxiv_id from the URL when the agent didn't supply one. Any non-'new'
    resolution counts as a dup (exact_hit / title_candidate / needs_better_copy).
    Failures are swallowed → returns None (treated as unverified).
    """
    cand_arxiv = (arxiv_id or "").strip()
    if not cand_arxiv and url:
        try:
            from collector.normalize import normalize_arxiv_id
            cand_arxiv = normalize_arxiv_id(url) or ""
        except Exception:
            cand_arxiv = ""
    if not (title or "").strip() and not cand_arxiv and not (doi or "").strip():
        return None
    try:
        from collector import gate
        resolution, wid = gate.light_gate({
            "title": title or "",
            "doi": doi or "",
            "arxiv_id": cand_arxiv,
        })
    except Exception:
        return None
    if resolution == "new" or not wid:
        return None
    return wid
```

- [ ] **Step 4：接入 `insert_discovery_hit`（`collector/discovery.py:507` 之后，`dedup_key` 计算之后、INSERT 之前）**

在现有 `if not url and not title:` 校验之后、`canonical_url`/`dedup_key` 计算之后，插入查重逻辑：

```python
    # works-level dedup (P0-2): metadata-only, never blocks insert.
    matched_work_id = _check_works_dup(
        title=title, doi=doi, arxiv_id=arxiv_id, url=url)
    if matched_work_id:
        verification_status = "dup_of_works"
        raw_json = dict(raw_json) if raw_json else {}
        raw_json.setdefault("dup_of_work_id", matched_work_id)
```

> 放在 run 内 dedup 检查（`if dedup_key:`）之前或之后均可，但必须在 INSERT 之前，使写入的 `verification_status` 生效。注意 `verification_status` 是函数形参默认 `"unverified"`，这里覆写。

- [ ] **Step 5：跑 core 测试到绿**

```bash
uv run python -m pytest tests/test_discovery_core.py -q
```

- [ ] **Step 6：给 `HitBackfillItem` 加 `doi`/`arxiv_id`（`api/routes/discovery.py:40-52`）**

在 `HitBackfillItem` 末尾（`verification_status` 之后）加两行：

```python
    doi: str = ""
    arxiv_id: str = ""
```

> `h.model_dump()` 随即带出这两个键 → `batch_insert_hits` → `insert_discovery_hit`（已支持该形参）。

- [ ] **Step 7：写 api 测试（强键路径）**

```python
def test_backfill_dup_by_arxiv_id_marked(client, sample_work_with_arxiv):
    """P0-2: agent 回填带 arxiv_id 且 works 已有该 arxiv_id → hit 标 dup_of_works。"""
    import json
    # sample_work_with_arxiv: works 表有一行 arxiv_id="2501.12345"
    run = client.post("/api/discovery/run", json={
        "mode": "composite", "input": {"mode": "composite"},
        "plan": {"queries": []}, "executor": "agent:web-access",
    }).json()
    r = client.post(f"/api/discovery/runs/{run['run_id']}/hits", json=[
        {"url": "https://arxiv.org/abs/2501.12345",
         "title": "Whatever", "arxiv_id": "2501.12345", "query": "q"}])
    assert r.status_code == 200
    hit = client.get(f"/api/discovery/hits?run_id={run['run_id']}").json()["hits"][0]
    assert hit["verification_status"] == "dup_of_works"
```

- [ ] **Step 8：跑全 discovery 套件回归 + healthcheck**

```bash
uv run python -m pytest tests/test_discovery_core.py tests/test_discovery_cli.py tests/test_discovery_api.py tests/test_discovery_smoke.py -q
uv run python scripts/healthcheck_library.py --json
```
预期：测试全 PASS；healthcheck 五项全零（不得引入新告警）。

- [ ] **Step 9：更新 agent 协议文档**

`docs/discovery-agent-protocol.md` 的回填字段表加 `doi`、`arxiv_id` 两列（可选但强烈建议填），并加一段说明：若回填的 hit 已在 works 中（强键或标题高度相似），后端会自动置 `verification_status="dup_of_works"`；agent 看到该标记的 hit 应**降低优先级或跳过**，不再重点推荐。

- [ ] **Step 10：commit**

```bash
git add collector/discovery.py api/routes/discovery.py tests/test_discovery_core.py tests/test_discovery_api.py docs/discovery-agent-protocol.md
git commit -m "feat(discovery): mark backfilled hits dup_of_works via light_gate (P0-2)"
```

---

### 任务 P1-1：发现检索页加「关联主题」下拉（打通 topic → run → hit → candidate 继承链）

**根因（已核对真实代码——这是关键纠错）：**
- 后端链路**已存在**：`CompositePlanBody.topic_id`（`api/routes/discovery.py:69`）✅；`RunBody.topic_id`（`:37`）→ `create_discovery_run(collection_topic_id=body.topic_id)`（`:173`）✅；`insert_discovery_hit` 继承 run 的 `collection_topic_id`（`collector/discovery.py:523-524`）✅；`accept_hit_to_intake` 把 `collection_topic_id` 带入 `intake_candidate`（`collector/discovery.py:783-791`）✅。
- **真正断在前端**：`DiscoveryReview.vue` 的 `doCompositePlan()`（`:340-345`）调用 `discoveryRun({mode, input, plan, executor})` 时**没有传 `topic_id`** → run 的 `collection_topic_id` 写空 → 链路断。
- 且主题输入是**自由文本**（`:14-16`），用户体验差，应为下拉。

**目标行为：**
1. 前端复合表单的「Topic ID」自由文本框 → 换成从 `getIntakeTopics` 拉取的 `<select>` 下拉（含「不关联」空选项）。
2. `doCompositePlan` 调 `discoveryRun` 时补 `topic_id: payload.topic_id`。
3. 端到端：选了主题 → run.collection_topic_id 写入 → hits 继承 → accept 后 intake_candidate 也带 topic_id。

**Files:**
- Modify: `web/src/views/DiscoveryReview.vue:12-16`（下拉）、`:317-353`（`doCompositePlan`）、`:222-230`（import）、`onMounted` 加载主题
- Modify: `web/src/api.js`（复用现有 `getIntakeTopics`，无需新增）
- Test: `tests/test_discovery_api.py`（端到端断言）

- [ ] **Step 1：写失败测试（api 端到端断言继承链）**

```python
def test_composite_run_with_topic_inherits_to_candidate(client, sample_topic):
    """P1-1: run 带 topic_id → hit 继承 → accept 后 intake_candidate 带 collection_topic_id。"""
    # 1. 建 run（带 topic_id）
    run = client.post("/api/discovery/run", json={
        "mode": "composite",
        "input": {"mode": "composite"},
        "plan": {"queries": ["q"]},
        "executor": "agent:web-access",
        "topic_id": sample_topic["id"],
    }).json()
    assert run["run_id"]
    # 2. 回填 hit
    client.post(f"/api/discovery/runs/{run['run_id']}/hits", json=[
        {"url": "https://arxiv.org/abs/2501.00001", "title": "Inherited Paper", "query": "q"}])
    hit = client.get(f"/api/discovery/hits?run_id={run['run_id']}").json()["hits"][0]
    assert hit["collection_topic_id"] == sample_topic["id"]
    # 3. accept
    client.post(f"/api/discovery/hits/{hit['id']}/accept", json={"review_note": ""})
    cand = client.get("/api/intake/candidates").json()["candidates"][0]
    assert cand["collection_topic_id"] == sample_topic["id"]
```

- [ ] **Step 2：跑红 → 先确认后端是否已通**

```bash
uv run python -m pytest tests/test_discovery_api.py::test_composite_run_with_topic_inherits_to_candidate -q
```
预期：**这条很可能直接 PASS**（后端链路已存在）。若 PASS，说明 P1-1 纯前端，跳到 Step 3；若 FAIL，修后端断点（最可能是 `accept_hit_to_intake` 没取到 hit 的 `collection_topic_id`，对照 `collector/discovery.py:783-791`）。

- [ ] **Step 3：前端 import 加 `getIntakeTopics`（`DiscoveryReview.vue:222-230`）**

把 import 改为：

```js
import { discoveryPlan, discoveryCompositePlan, discoveryRun, getDiscoveryRuns,
  getDiscoveryHits, acceptDiscoveryHit, rejectDiscoveryHit, getIntakeTopics } from '../api'
```

- [ ] **Step 4：加主题列表 ref 并在 onMounted 加载（script setup 区，`composite` ref 附近）**

```js
const topics = ref([])
async function loadTopics() {
  try {
    const data = await getIntakeTopics({ per_page: 200 })
    topics.value = data.topics || data || []
  } catch (e) { topics.value = [] }
}
```

在 `onMounted`（`:509-513`）里追加 `loadTopics()` 调用。

- [ ] **Step 5：把「Topic ID」自由文本框换成下拉（`DiscoveryReview.vue:13-16`）**

把这段：

```html
        <div class="form-row">
          <label class="form-label">Topic ID</label>
          <input v-model="composite.topic_id" placeholder="topic_id (可选)" class="form-input form-input-sm" />
        </div>
```

替换为：

```html
        <div class="form-row">
          <label class="form-label">关联主题（可选）</label>
          <select v-model="composite.topic_id" class="form-select">
            <option value="">不关联主题</option>
            <option v-for="t in topics" :key="t.id" :value="t.id">
              {{ t.name }}（{{ t.map_status }}）
            </option>
          </select>
        </div>
```

- [ ] **Step 6：`doCompositePlan` 的 `discoveryRun` 补 `topic_id`（`DiscoveryReview.vue:340-345`）**

把这段：

```js
    const run = await discoveryRun({
      mode: 'composite',
      input: inputSignals,
      plan,
      executor: 'agent:web-access',
    })
```

替换为：

```js
    const run = await discoveryRun({
      mode: 'composite',
      input: inputSignals,
      plan,
      executor: 'agent:web-access',
      topic_id: payload.topic_id || undefined,
    })
```

- [ ] **Step 7：构建 + 视觉验证**

```bash
cd D:/02_academic/doctoral/literature_library/web && npm.cmd run build
```
预期：构建成功。手测：打开发现检索页 → 复合表单顶部出现「关联主题」下拉 → 选一个主题 → 生成方案 → 后端 run 的 `collection_topic_id` 非空（可查 `/api/discovery/runs/{id}`）。

- [ ] **Step 8：commit**

```bash
git add web/src/views/DiscoveryReview.vue tests/test_discovery_api.py
git commit -m "feat(web): composite form topic selector + pass topic_id to run (P1-1)"
```

---

### 任务 P1-2：建主题时即可选分类标签（含自定义 proposed_new 标签）

**根因（已核对真实代码）：**
- `collector/topics.py:60-92` 的 `create()` **不接受** `mapped_tags`（硬编码 `None`），只内部构造 `query_def={explicit_ids, seed_paper_ids}`。
- `api/routes/intake.py:280-301` 的 `TopicCreateBody` 也没有 tags 字段。
- `collector/topics.py` 的 mapped_tags 校验（约 27-44 行）用 `validate_tag_value` **拒绝任何不在词表里的值** → 无法支持「研究方向不在现有 risk_domain 里」的自定义场景。
- 词表 `api/classification_vocab.py` 是静态 dict，**无 per-value status**。所以 proposed_new 状态只能存在主题的 `mapped_tags` JSON 里（`{group, value, status}`），不能动词表本体。

**目标行为：**
1. `mapped_tags` 条目结构升级为 `{group, value, status}`，status ∈ {`"approved"`（在词表内）/ `"proposed_new"`（自定义）}。
2. `topics.create` 接受可选 `mapped_tags: list[dict]`，按词表自动判定每条 status；`map_status` 仍为 `seedling`。
3. 创建端点 Body 与前端 createForm 同步支持。
4. 前端列表现有「?」警告（`TopicsReview.vue` 的 `isVocabValue` 判断）天然标识 proposed_new，无需额外样式即可区分。

**Files:**
- Modify: `collector/topics.py:60-92`（`create` 签名扩展 + mapped_tags status 标注 + 校验放宽）
- Modify: `api/routes/intake.py:280-301`（`TopicCreateBody` 加字段）
- Modify: `web/src/views/TopicsReview.vue`（createForm 加标签选择区）
- Test: `tests/`（找现有 topics 测试文件，如 `tests/test_intake_topics.py` 或类似）

- [ ] **Step 1：定位现有 topics 测试文件**

```bash
uv run python -m pytest tests/ -q -k "topic" --collect-only
```
找到后在其内追加测试；若无，建 `tests/test_topics_create_tags.py`。

- [ ] **Step 2：写失败测试**

```python
def test_create_topic_with_vocab_tags_marks_approved(hermetic_db):
    """P1-2: 创建时带词表内标签 → status=approved，map_status 仍 seedling。"""
    from collector import topics
    ct = topics.create(
        name="T1", description="",
        mapped_tags=[{"group": "risk_domain", "value": "reward_hacking"}],
    )  # reward_hacking 须是词表内真值；按实际词表替换
    assert ct["map_status"] == "seedling"
    tags = ct["mapped_tags"]
    assert tags[0]["status"] == "approved"


def test_create_topic_with_custom_tag_marks_proposed_new(hermetic_db):
    """P1-2: 词表外的自定义值 → status=proposed_new，不报错。"""
    from collector import topics
    ct = topics.create(
        name="T2", description="",
        mapped_tags=[{"group": "risk_domain", "value": "goal_misgeneralization"}],
    )
    tags = ct["mapped_tags"]
    assert tags[0]["status"] == "proposed_new"
    assert tags[0]["value"] == "goal_misgeneralization"


def test_create_topic_without_tags_keeps_mapped_tags_none(hermetic_db):
    """P1-2: 不带标签 → mapped_tags 仍为 None（兼容旧路径）。"""
    from collector import topics
    ct = topics.create(name="T3")
    assert ct["mapped_tags"] is None
```

- [ ] **Step 3：跑红**

```bash
uv run python -m pytest tests/test_topics_create_tags.py -q
```
预期：FAIL（`create()` 不接受 `mapped_tags`）。

- [ ] **Step 4：改 `topics.create`（`collector/topics.py:60-92`）**

把签名与函数体改为（保留原有 `query_def` 构造不动）：

```python
def create(*, name, description="", seed_paper_ids=None, explicit_ids=None,
           axis_hint=None, mapped_tags=None, map_status="seedling", lifecycle="active"):
    """Create a collection_topic. Defaults to seedling/active.

    query_def captures the discovery inputs that seed this topic:
      - explicit_ids:   paper IDs the user explicitly handed in
      - seed_paper_ids: paper IDs chosen as seeds (e.g. from a hunch)

    mapped_tags (P1-2): optional list of {group, value} (status auto-filled).
    Values in vocab get status='approved'; out-of-vocab get 'proposed_new'
    (human review later decides whether to admit into classification_vocab).
    """
    if map_status not in ALLOWED_MAP:
        raise ValueError(f"bad map_status {map_status}")
    if lifecycle not in ALLOWED_LIFE:
        raise ValueError(f"bad lifecycle {lifecycle}")
    qd = {"explicit_ids": explicit_ids or [], "seed_paper_ids": seed_paper_ids or []}

    mapped_tags_json = None
    if mapped_tags:
        from api.classification_vocab import validate_tag_value
        norm = []
        for t in mapped_tags:
            g, v = (t.get("group") or "").strip(), (t.get("value") or "").strip()
            if not g or not v:
                continue  # skip malformed
            status = "approved" if validate_tag_value(g, v) else "proposed_new"
            norm.append({"group": g, "value": v, "status": status})
        mapped_tags_json = json.dumps(norm, ensure_ascii=False) if norm else None

    conn = get_conn()
    try:
        ct = {
            "id": _new_id(), "name": name, "description": description,
            "query_def": qd, "map_status": map_status, "lifecycle": lifecycle,
            "mapped_tags": mapped_tags_json, "proposed_note": None, "axis_hint": axis_hint,
            "created_at": _now(), "updated_at": _now(),
        }
        conn.execute(
            """INSERT INTO collection_topics
               (id,name,description,query_def,map_status,lifecycle,mapped_tags,
                proposed_note,axis_hint,created_at,updated_at)
               VALUES (?,?,?,?,?,?,?,?,?,?,?)""",
            (ct["id"], ct["name"], ct["description"], json.dumps(qd, ensure_ascii=False),
             ct["map_status"], ct["lifecycle"], mapped_tags_json, None, ct["axis_hint"],
             ct["created_at"], ct["updated_at"]))
        conn.commit()
        ct["mapped_tags"] = json.loads(mapped_tags_json) if mapped_tags_json else None
        return ct
    finally:
        conn.close()
```

> **注意**：原先 create 把 `mapped_tags` 在返回 dict 里设为 `None`（Python None）。现在返回解析后的 list（与 `get()` 行为一致）。后续 `transitionTopic` 推进到 mapped 时若再写 mapped_tags，也必须用同一 `{group,value,status}` 结构（见该文件 `transition`/`update` 函数，对照修改使其兼容 status 字段——若该函数剥离 status，需保留）。

- [ ] **Step 5：跑 core 到绿**

```bash
uv run python -m pytest tests/test_topics_create_tags.py -q
```

- [ ] **Step 6：扩展 `TopicCreateBody` 与端点（`api/routes/intake.py:280-301`）**

```python
class TopicCreateBody(BaseModel):
    name: str
    description: str = ""
    seed_paper_ids: list[str] | None = None
    explicit_ids: list[str] | None = None
    axis_hint: str | None = None
    mapped_tags: list[dict] | None = None
```

端点 `intake_topics_create` 内 `topics.create(...)` 调用补 `mapped_tags=body.mapped_tags`。

- [ ] **Step 7：前端 createForm 加「关联领域」区（`TopicsReview.vue` createForm，约 `:117-145`）**

在 `axis_hint` 字段之后、提交按钮之前加：

```html
        <div class="form-row">
          <label class="form-label">关联领域（可选，多选）</label>
          <div class="tag-picker">
            <select v-model="tagPicker.group" class="form-select" @change="tagPicker.value=''">
              <option value="risk_domain">risk_domain</option>
              <option value="reading_lane">reading_lane</option>
              <option value="method_tags">method_tags</option>
            </select>
            <select v-if="tagPicker.group" v-model="tagPicker.value" class="form-select">
              <option value="">— 从词表选 —</option>
              <option v-for="v in (vocabValues(tagPicker.group))" :key="v" :value="v">{{ v }}</option>
            </select>
            <input v-model="tagPicker.custom" placeholder="或输入自定义值（申请新增）" class="form-input" />
            <button type="button" class="btn-tag-add" @click="addCreateTag">添加</button>
          </div>
          <div class="tag-chips">
            <span v-for="(t,i) in createForm.mapped_tags" :key="i" class="tag-chip" :class="{ proposed: t.status==='proposed_new' }">
              {{ t.group }}:{{ t.value }}{{ t.status==='proposed_new' ? ' ?' : '' }}
              <button type="button" @click="removeCreateTag(i)">×</button>
            </span>
          </div>
        </div>
```

script 区：

```js
const tagPicker = ref({ group: 'risk_domain', value: '', custom: '' })
// createForm 增加 mapped_tags: []
// （在 createForm 初始化处补 mapped_tags: []）
function vocabValues(group) {
  return (vocab.value && vocab.value[group]) || []
}
function addCreateTag() {
  const g = tagPicker.value.group
  const v = (tagPicker.value.custom || '').trim() || tagPicker.value.value
  if (!g || !v) return
  if (createForm.value.mapped_tags.some(t => t.group===g && t.value===v)) return
  const inVocab = vocabValues(g).includes(v)
  createForm.value.mapped_tags.push({ group: g, value: v, status: inVocab ? 'approved' : 'proposed_new' })
  tagPicker.value.custom = ''; tagPicker.value.value = ''
}
function removeCreateTag(i) { createForm.value.mapped_tags.splice(i, 1) }
```

`doCreateTopic` 提交时 payload 带 `mapped_tags: createForm.value.mapped_tags.length ? createForm.value.mapped_tags : null`，并需在 `getVocab()` onMounted 已加载（现有代码已加载 vocab）。

> **注意**：`vocab` 当前在打开 map modal 时才加载（`openMapModal` → `getVocab`）。createForm 也要用，故把 `getVocab` 提前到 `onMounted` 一并调用。

- [ ] **Step 8：构建 + 手测**

```bash
cd D:/02_academic/doctoral/literature_library/web && npm.cmd run build
```
手测：新建主题 → 选 risk_domain 词表值（显示无 ?）+ 输入自定义值（显示 `?`，proposed）→ 创建 → 列表里该主题 mapped_tags 带两种。

- [ ] **Step 9：commit**

```bash
git add collector/topics.py api/routes/intake.py web/src/views/TopicsReview.vue tests/
git commit -m "feat(topics): create with mapped_tags + proposed_new custom values (P1-2)"
```

---

### 任务 P1-3：主题 query_def 复合字段扩展（存得下 + 从主题出发生成 plan 时读得到）

**根因（已核对真实代码——这是第二个关键纠错）：**
- `topics.create` 只往 query_def 写 `{explicit_ids, seed_paper_ids}`（`collector/topics.py:72`）。
- 发现层读 query_def 的键是 `keywords`/`known_names`/`known_titles`（`collector/discovery.py:119-122` 与 `api/routes/discovery.py:94-103`）——**键名对不上**，所以从主题出发的检索拿不到任何线索。
- **更关键**：`draft_composite_plan`（`collector/discovery.py:180-395`）**根本不读 topic.query_def**，`topic_id` 只被原样塞进 `input_signals`（`:383`）。`/discovery/composite-plan` 路由（`:125-149`）也不加载 query_def。

**目标行为：**
1. query_def schema 扩展为完整线索集（新旧键共存，代码做 `.get(key) or []` 兜底）。
2. `topics.create` 接受可选 `query_def: dict`，合并到默认 `{explicit_ids, seed_paper_ids}` 之上。
3. 新增 `topics.update_query_def` + `PATCH /intake/topics/{id}/query-def`，支持编辑现有主题线索（反哺闭环 P3-2 需要，但端点先建好）。
4. `/discovery/composite-plan` 路由在 `topic_id` 非空时加载 topic.query_def，作为 names/titles/authors/institutions/keywords/known_urls/preferred_domains/exclude_terms 的**默认底值**（调用方显式传的值优先合并）。
5. 前端 createForm 加可折叠「高级线索」区；详情面板加「编辑线索」入口（最小可用）。

**query_def 目标 schema：**
```json
{
  "explicit_ids": ["arXiv:1234"],
  "seed_paper_ids": ["WK-xxxx"],
  "keywords": ["safety evaluation"],
  "known_names": ["GPT-4"],
  "known_titles": ["system card"],
  "authors": ["Alice Smith"],
  "institutions": ["OpenAI"],
  "known_urls": ["https://openai.com/..."],
  "preferred_domains": ["arxiv.org"],
  "exclude_terms": ["blog"]
}
```

**Files:**
- Modify: `collector/topics.py`（`create` 加 `query_def` 形参 + 新增 `update_query_def`）
- Modify: `collector/discovery.py:180-395`（`draft_composite_plan` 加 topic 合并）
- Modify: `api/routes/discovery.py:125-149`（composite-plan 路由加载 query_def）
- Modify: `api/routes/intake.py`（`TopicCreateBody` 加 `query_def`；新增 PATCH 端点）
- Modify: `web/src/views/TopicsReview.vue`（createForm 高级区 + 详情编辑）
- Modify: `docs/discovery-agent-protocol.md`（topic → plan 取字段说明）
- Test: `tests/`

- [ ] **Step 1：写失败测试（core：存得下 + plan 读得到）**

```python
def test_create_topic_stores_rich_query_def(hermetic_db):
    """P1-3: 创建时带复合 query_def → get() 能取到所有键。"""
    from collector import topics
    ct = topics.create(
        name="T", query_def={"keywords": ["safety"], "authors": ["Alice"],
                             "institutions": ["OpenAI"], "known_urls": ["https://x.org"]})
    qd = topics.get(ct["id"])["query_def"]
    assert qd["keywords"] == ["safety"]
    assert qd["authors"] == ["Alice"]
    assert qd["explicit_ids"] == []  # 默认键仍在


def test_update_query_def_merges(hermetic_db):
    """P1-3: update_query_def 覆盖传入键，保留未传键。"""
    from collector import topics
    ct = topics.create(name="T", query_def={"keywords": ["a"]})
    topics.update_query_def(ct["id"], {"authors": ["Bob"]})
    qd = topics.get(ct["id"])["query_def"]
    assert qd["keywords"] == ["a"]      # 旧键保留
    assert qd["authors"] == ["Bob"]     # 新键写入


def test_composite_plan_reads_topic_query_def(hermetic_db):
    """P1-3: 仅给 topic_id（无显式线索）→ plan 的 queries 来自 topic.query_def.keywords。"""
    from collector import topics, discovery
    ct = topics.create(name="Reward Hacking", query_def={"keywords": ["reward hacking"]})
    plan = discovery.draft_composite_plan(topic_id=ct["id"])
    # plan.queries 应反映 reward hacking 线索（非空）
    assert any("reward hacking" in q.lower() for q in plan.get("queries", []))
```

- [ ] **Step 2：跑红**

```bash
uv run python -m pytest tests/ -q -k "rich_query_def or update_query_def or reads_topic_query_def"
```
预期：FAIL。

- [ ] **Step 3：`topics.create` 加 `query_def` 形参（`collector/topics.py:60-92`，与 P1-2 改动叠加）**

签名追加 `query_def: dict | None = None`，并把 `qd` 构造改为：

```python
    qd = {"explicit_ids": explicit_ids or [], "seed_paper_ids": seed_paper_ids or []}
    if query_def:
        for k, v in query_def.items():
            if isinstance(v, list):
                qd[k] = v
            elif v:
                qd[k] = [v]
```

- [ ] **Step 4：新增 `topics.update_query_def`（`collector/topics.py`，`get` 之后）**

```python
def update_query_def(topic_id, patch: dict):
    """Merge patch keys into topic.query_def (lists only). Preserves absent keys.
    Raises KeyError if topic missing. Returns the new query_def dict."""
    conn = get_conn()
    try:
        cur = conn.execute("SELECT query_def FROM collection_topics WHERE id=?", (topic_id,))
        row = cur.fetchone()
        if not row:
            raise KeyError(f"topic not found: {topic_id}")
        qd = json.loads(row[0] or "{}")
        for k, v in (patch or {}).items():
            if isinstance(v, list):
                qd[k] = v
            elif v:
                qd[k] = [v]
            else:
                qd.pop(k, None)
        conn.execute(
            "UPDATE collection_topics SET query_def=?, updated_at=? WHERE id=?",
            (json.dumps(qd, ensure_ascii=False), _now(), topic_id))
        conn.commit()
        return qd
    finally:
        conn.close()
```

- [ ] **Step 5：`draft_composite_plan` 读 topic.query_def 作底值（`collector/discovery.py:180-395`）**

在函数体开头（校验之后、构造 queries 之前）插入 topic 加载与合并：

```python
    # P1-3: when a topic is given, seed defaults from its query_def so a topic
    # alone is enough to drive a plan. Explicit kwargs still win (merged on top).
    if topic_id:
        from collector import topics
        topic = topics.get(topic_id)
        if topic is None:
            raise KeyError(f"topic not found: {topic_id}")
        qd = topic.get("query_def") or {}
        names = _merge_list(qd.get("known_names"), names)
        titles = _merge_list(qd.get("known_titles"), titles)
        authors = _merge_list(qd.get("authors"), authors)
        institutions = _merge_list(qd.get("institutions"), institutions)
        keywords = _merge_list(qd.get("keywords"), keywords)
        known_urls = _merge_list(qd.get("known_urls"), known_urls)
        preferred_domains = _merge_list(qd.get("preferred_domains"), preferred_domains)
        exclude_terms = _merge_list(qd.get("exclude_terms"), exclude_terms)
```

在文件工具函数区新增（与 `_canonicalize_url` 等同层级）：

```python
def _merge_list(base, extra):
    """Merge two list-ish inputs, dedup, preserve order (base first, extra after)."""
    out = []
    for x in list(base or []) + list(extra or []):
        if x and x not in out:
            out.append(x)
    return out
```

> 放在 `draft_composite_plan` **之前**或函数定义之前均可；保证合并发生在构造 queries 之前即可。

- [ ] **Step 6：跑 core 到绿**

```bash
uv run python -m pytest tests/ -q -k "rich_query_def or update_query_def or reads_topic_query_def"
uv run python -m pytest tests/test_discovery_core.py -q
```

- [ ] **Step 7：API 层：`TopicCreateBody` 加 `query_def` + 新增 PATCH 端点（`api/routes/intake.py`）**

`TopicCreateBody` 追加：

```python
    query_def: dict | None = None
```

`intake_topics_create` 调用补 `query_def=body.query_def`。

新增端点：

```python
class TopicQueryDefBody(BaseModel):
    patch: dict

@router.patch("/intake/topics/{topic_id}/query-def")
def intake_topics_patch_query_def(topic_id: str, body: TopicQueryDefBody):
    try:
        qd = topics.update_query_def(topic_id, body.patch)
    except KeyError as e:
        raise HTTPException(404, str(e))
    return {"ok": True, "topic_id": topic_id, "query_def": qd}
```

- [ ] **Step 8：前端 createForm 加可折叠高级线索区（`TopicsReview.vue`）**

在 createForm 的 axis_hint / mapped_tags 区之后加一个 `<details>` 折叠块，每字段一个 textarea（换行分隔），绑定 `createForm.query_*` 字符串；提交时 `doCreateTopic` 把非空的拼成数组塞进 `query_def`。具体字段：keywords / known_names / known_titles / authors / institutions / known_urls / preferred_domains / exclude_terms。

```html
        <details class="advanced-clues">
          <summary>高级检索线索（可选）</summary>
          <div class="form-row"><label>Keywords</label>
            <textarea v-model="createForm.q_keywords" placeholder="每行一个"></textarea></div>
          <div class="form-row"><label>已知名称 (known_names)</label>
            <textarea v-model="createForm.q_known_names" placeholder="每行一个"></textarea></div>
          <!-- 其余 6 个字段同构：known_titles / authors / institutions / known_urls / preferred_domains / exclude_terms -->
        </details>
```

`doCreateTopic` 提交前：

```js
function buildQueryDef() {
  const qd = {}
  const map = { keywords: createForm.value.q_keywords, known_names: createForm.value.q_known_names,
    known_titles: createForm.value.q_known_titles, authors: createForm.value.q_authors,
    institutions: createForm.value.q_institutions, known_urls: createForm.value.q_known_urls,
    preferred_domains: createForm.value.q_preferred_domains, exclude_terms: createForm.value.q_exclude_terms }
  for (const [k, v] of Object.entries(map)) {
    const arr = (v || '').split('\n').map(s => s.trim()).filter(Boolean)
    if (arr.length) qd[k] = arr
  }
  return Object.keys(qd).length ? qd : null
}
```

payload 带 `query_def: buildQueryDef()`。

- [ ] **Step 9：构建 + 手测 + 文档**

```bash
cd D:/02_academic/doctoral/literature_library/web && npm.cmd run build
```

`docs/discovery-agent-protocol.md` 新增「主题 → 检索方案」一节，列明从 query_def 取的字段映射（known_names→names、known_titles→titles、其余同名），并说明调用方可覆盖。

- [ ] **Step 10：commit**

```bash
git add collector/topics.py collector/discovery.py api/routes/intake.py api/routes/discovery.py web/src/views/TopicsReview.vue docs/discovery-agent-protocol.md tests/
git commit -m "feat(topics): rich query_def + composite-plan reads topic clues (P1-3)"
```

---

### 任务 P2-1：侧边栏布局重排（流程导向分组）

**根因：** `web/src/components/AppLayout.vue:11-40` 硬编码 10 个 `<router-link>`，顺序为：总览/文献/去重/关系/元数据/分类审核/采集审核/收件箱摄入/主题闸门/发现检索。流程入口（主题/发现/收件箱）被埋在底部。

**目标新顺序（带分组分隔）：**
```
顶层：  总览 / 文献
流程：  主题闸门 / 发现检索 / 收件箱摄入 / 采集审核 / 分类审核 / 元数据
库内：  去重 / 关系
```

**Files:** `web/src/components/AppLayout.vue:11-40`

- [ ] **Step 1：重排 router-link 并加分组标签**

把 `:11-40` 的 nav 区改为（保留原有 active class 逻辑与图标，仅调顺序 + 插入 `<li class="nav-group">` 分隔标签）：

```html
<nav class="sidebar-nav">
  <router-link to="/" active-class="active">📊 总览</router-link>
  <router-link to="/works" active-class="active">📚 文献</router-link>

  <li class="nav-group">流程</li>
  <router-link to="/topics" active-class="active">🗂️ 主题闸门</router-link>
  <router-link to="/discovery" active-class="active">🔎 发现检索</router-link>
  <router-link to="/inbox" active-class="active">📥 收件箱摄入</router-link>
  <router-link to="/intake" active-class="active">📋 采集审核</router-link>
  <router-link to="/classification" active-class="active">🏷️ 分类审核</router-link>
  <router-link to="/metadata" active-class="active">📝 元数据</router-link>

  <li class="nav-group">库内</li>
  <router-link to="/duplicates" active-class="active">🔍 去重</router-link>
  <router-link to="/relations" active-class="active">🔗 关系</router-link>
</nav>
```

- [ ] **Step 2：加 `.nav-group` 样式**

在 AppLayout.vue `<style>` 内（`:root` 之外）加：

```css
.nav-group {
  list-style: none;
  font-size: 11px;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--muted);
  padding: 16px 16px 4px;
  margin: 0;
}
```

> `<nav>` 内直接放 `<li>` 非严格嵌套，但 Vue 模板允许且项目无 lint 强制；若担心，可把 `<li class="nav-group">` 换成 `<div class="nav-group">`。

- [ ] **Step 3：构建 + 视觉验证**

```bash
cd D:/02_academic/doctoral/literature_library/web && npm.cmd run build
```
手测：侧边栏顺序为 总览/文献 → 流程组 → 库内组；各链接 active 高亮正常；折叠态正常。

- [ ] **Step 4：commit**

```bash
git add web/src/components/AppLayout.vue
git commit -m "style(web): flow-oriented sidebar grouping (P2-1)"
```

---

### 任务 P2-2：两个按钮语义优化

**根因：** `TopicsReview.vue` 第 `:80` `按主题发起采集` 与 `:89` `生成检索方案` 区分不直观。

**目标：** 改名 + 加副标题/tooltip，让新用户秒懂两条路径差异。

**Files:** `web/src/views/TopicsReview.vue:80`、`:89`

- [ ] **Step 1：改名 + 加 title tooltip**

把 `:89` 的「生成检索方案」按钮改为：

```html
<button class="btn-plan" @click="doDiscoveryPlan"
        title="让 AI 根据主题线索（关键词/作者/机构等）联网搜索相关文献，结果进发现命中审核">
  智能检索
</button>
```

把 `:80` 的「按主题发起采集」按钮改为：

```html
<button class="btn-collect" @click="doCollect"
        title="按主题里已有的显式 ID / 种子 ID 直接下载指定文献，不联网搜索">
  直接采集
</button>
```

- [ ] **Step 2：（可选）按钮下加一行小字说明**

在两个按钮所在 gate-bar 下方加：

```html
<p class="gate-hint">
  <strong>智能检索</strong>：AI 联网找相关文献 ｜ <strong>直接采集</strong>：按已知 ID 下载
</p>
```

加样式 `.gate-hint { font-size:12px; color:var(--muted); margin:4px 0 8px; }`。

- [ ] **Step 3：构建 + 手测**

```bash
cd D:/02_academic/doctoral/literature_library/web && npm.cmd run build
```

- [ ] **Step 4：commit**

```bash
git add web/src/views/TopicsReview.vue
git commit -m "style(topics): rename discovery/collect buttons with tooltips (P2-2)"
```

---

### 任务 P2-3：前端 UI/UX 审计与重构方案（**仅产出文档，禁止写任何业务代码**）

> **硬约束：本任务不修改任何 .vue / .js / .css 业务文件。只读审计 + 产出一份设计文档。**

**Files（只读）：** `web/src/views/*.vue`、`web/src/components/*.vue`、`web/src/App.vue`
**Files（产出）：** `docs/superpowers/reviews/2026-07-01-frontend-design-audit.md`（新建）

- [ ] **Step 1：逐页截图与现状审计**

对 8 个页面（DiscoveryReview / TopicsReview / InboxReview / IntakeReview / MetadataReview / ClassificationReview / Works / Dashboard）逐页记录：视觉一致性、组件复用、布局合理性、交互反馈、响应式。用 playwright-skill 截图（dev server 起后）。

- [ ] **Step 2：产出设计令牌草案**

基于 `AppLayout.vue:54-66` 现有 10 个 CSS 变量（`--bg/--panel/--line/--text/--muted/--accent/--ok/--warn/--bad/--chip`），补全圆角/阴影/间距阶梯/状态色变体的命名草案（不落地，只写在文档里）。

- [ ] **Step 3：产出公共组件候选清单**

列出 TagChip / StatusBadge / ConfirmDialog / EmptyState / LoadingSpinner / FieldGroup / ActionToolbar / PaginationNav 的来源页与职责。

- [ ] **Step 4：产出双面板布局线框图**

左侧流程面板（240-280px，可折叠为图标）+ 右侧内容面板 + 顶部 Tab（文献库视图 / 流程视图）的 ASCII 线框。

- [ ] **Step 5：落盘审计文档**

写入 `docs/superpowers/reviews/2026-07-01-frontend-design-audit.md`，含：审计报告 + 设计令牌草案 + 公共组件清单 + 双面板线框 + 优先级排序的落地建议。文档末尾声明「本任务零代码变更」。

- [ ] **Step 6：commit**

```bash
git add docs/superpowers/reviews/2026-07-01-frontend-design-audit.md
git commit -m "docs(web): frontend design audit & refactor proposal (P2-3, planning only)"
```

---

## 四、分阶段实施路线

| 阶段 | 任务 | 互相依赖 | 完成判据 |
|:----:|------|:--------:|----------|
| P0 | P0-1, P0-2 | 可并行 | 两条 core+api 测试通过、healthcheck 全零 |
| P1 | P1-1, P1-2, P1-3 | 可并行（P1-2/P1-3 同改 topics.py，建议串行避免冲突） | 各自测试通过、npm build 通过 |
| P2 | P2-1, P2-2, P2-3 | 可并行 | npm build 通过 + P2-3 文档落盘 |
| P3 | 远期，暂不启动 | 依赖 P0-P2 | — |

> **P1-2 与 P1-3 都改 `collector/topics.py` 的 `create()` 与周边**：若同一实施会话内并行，**必须串行合并**（先 P1-2 后 P1-3，或反之），不要开两个 worktree 同时改同一函数。

---

## 五、边界约束（绝对不做）

1. ❌ 不自动 accept/promote 任何 hit 或 candidate
2. ❌ 不改词表自动审批（proposed_new 必须人工审核后才能进 `classification_vocab`）
3. ❌ 不做无约束全网扫描（queries 上限 10，max_results 上限 100，已在 `draft_composite_plan` 内强制）
4. ❌ 不走 DOI/arXiv/GitHub 作为 discovery mode（`UNSUPPORTED_MODES` 已拦）
5. ❌ 不下载 PDF 到 discovery 层（P0-2 只用 `light_gate`，绝不调 `heavy_gate`）
6. ❌ 不修改已有的 topic/name/title/url 单模式 API
7. ❌ 不做全自动定时入库
8. ❌ P2-3 阶段不写任何业务代码

---

## 六、风险

| 风险 | 概率 | 影响 | 缓解 |
|------|:----:|:----:|------|
| proposed_new 标签膨胀 | 中 | 低 | status 字段 + 定期人工审核 + 不影响查询 |
| query_def 旧数据缺新键 | 低 | 中 | JSON text 新旧共存，代码全用 `.get(key) or []` |
| light_gate 全表扫 title 增回填延迟 | 低 | 低 | works 仅 146 行，毫秒级；不下载 PDF |
| 侧边栏重排影响习惯 | 中 | 低 | 仅顺序调整，功能不变 |
| P1-2/P1-3 并行改 topics.create 冲突 | 中 | 中 | 同会话内串行，不开双 worktree 改同函数 |

---

## 七、验收总关口

每个阶段完成后**三行命令全过才算完成**：

```bash
# 1. 后端 discovery 套件
uv run python -m pytest tests/test_discovery_core.py tests/test_discovery_cli.py tests/test_discovery_api.py tests/test_discovery_smoke.py -q

# 2. 前端构建
cd D:/02_academic/doctoral/literature_library/web && npm.cmd run build

# 3. 健康检查
cd D:/02_academic/doctoral/literature_library && uv run python scripts/healthcheck_library.py --json
```

P1 完成后追加全量回归：

```bash
uv run python -m pytest tests/ -q --ignore=tests/test_integration_heavy.py
```

---

## 八、与其他未来工作的关系

| 本计划项 | 依赖它的未来工作 |
|----------|----------------|
| P1-2（主题选分类标签） | 分类规范与审核模板 V1.1 |
| P1-3（query_def 扩展 + update 端点） | 元数据反哺主题线索（P3-2）、采集检索模板 |
| P0-2（回填查重） | needs_better_copy 替换链路 |
| P0-1（/complete 端点 + 状态机） | Discovery 本地模型自动执行器、P3 重新执行 |
| P2-3（设计审计） | 前端公共组件化、双面板布局 |

---

> **本文档是 `FUTURE_WORK_PLAN.md` 最高优先级的实施依据。**
> 实施进度与每阶段产出见配套实施文档 `docs/superpowers/plans/2026-06-30-v1.3-handoff.md`（即交接/实施入口）。
> 阶段审核结果写入 `docs/superpowers/reviews/2026-07-01-v1.3-p{阶段}-result.md`。
